## Archive canonical claims ledger
This file is a stricter canonical ledger for the archive corpus in `research/agent_handoffs/combined_all_variants` only. Archive files are the extraction corpus. Current Hydra docs/code are used only for post-extraction validation and contradiction checks.
## Coverage
- total archive files processed: 28 non-reference `answer_*` combined archive files
- total archive files skipped: 3 `reference_prompt_example_*` files, skipped because they are prompt exemplars rather than prompt+answer claim records
- total raw claims extracted internally: 1070 candidate claim lines/blocks from the 28 processed archive files
- total canonical claims kept: 35
- total claims merged away: 1035 raw candidates collapsed during dedupe into duplicate rows or supporting/predecessor material for retained rows
- total claims rejected: 3 canonical claims retained only as reject/block or rejected-precision rows
- total unresolved / uncertain canonical claims: 14 rows still kept at `partially supported`, `repo-blocked`, `trust medium`, `ready later`, or `code unclear` after the stricter pass
- processed-file outcome accounting: 22 files contributed surviving claim(s); 2 files contributed reject/block claim(s); 3 files were merged away into other canonical rows; 1 processed file produced no surviving canonical claim
- coverage note: accounting here is file-exhaustive for the 28 processed `answer_*` files, but extraction was block-based rather than sentence-exhaustive. Early files were re-checked in this pass and are either cited into surviving rows or classified explicitly in the appendix.
## Legend
- `all_source_refs`: exact archive file and line spans supporting the canonical claim
- `repo_supported`: whether current Hydra docs/code support the claim judgment after extraction; status is explained in `repo_support_detail`
- `hydra_docs_present`: whether current Hydra docs explicitly carry the doctrine/claim; status is explained in `hydra_docs_detail`
- `in_code_now`: whether current code implements the claim substance today; status is explained in `in_code_detail`
- `reproduced`: whether the claim was independently reconstructed from archive lines plus current repo evidence; status is explained in `reproduced_detail`
- each row was re-checked through three internal validation passes; `validated_pass_2` records the strictest surviving visible judgment after pass-2 and pass-3 tightening so the table schema stays unchanged
| canonical_claim | tag | all_source_refs | supporting_source_quotes | repo_supported | repo_support_detail | hydra_docs_present | hydra_docs_detail | in_code_now | in_code_detail | reproduced | reproduced_detail | validated_pass_1 | validated_pass_2 | trustworthy | implementation_ready | promising | strength_upside | risk | fallback_worthy | fallback_role | notes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| The current `safety_residual` target object is semantically wrong in the archive-criticized/currently implemented form: it is built from a clipped `public_score - exact_risk` quantity rather than a true signed residual. | targets | `answer_18_combined.md:1294-1469`<br>`answer_18_combined.md:1848-1857`<br>`answer_2_combined.md:289-295` | “That is not a residual. It is either the unchanged public score or zero.”<br>“Use `Δ_s(a)` as the supervised target.”<br>“do **not** target exact danger directly” | supported in repo | Repo directly supports this defect claim because the current replay loader already builds the target family and the implemented target algebra matches the criticized clipped public-minus-exact-risk form. The support is not just that a target slot exists; it is that the wrong object is visibly constructed in the current builder and then carried through the existing masked training path. | docs absent | Current Hydra docs discuss advanced-target closure and caution around weak target semantics, but they do not explicitly document this exact algebraic defect in the current `safety_residual` target object or name the clipped public-minus-exact-risk quantity as a problem. | partly in code | The path is live end-to-end in training in the narrow sense that the target object, mask, batch carrier, head, and masked loss all exist today; it is only `partial` because the semantics of the built target are the criticized old version rather than the archive’s corrected version. | independently reproduced | Independently reconstructed by reading the archive critique, then checking the current replay builder, batch carrier, and masked-loss path to confirm three things locally: the built quantity is clipped public-minus-exact-risk, that quantity is not a signed correction around public score, and the same object really is what current training consumes. | Pass 1: archive wording and current code agree that the shipped target algebra is not a true residual and therefore misnames the object it trains. | Pass 2 + 3: keep the row strictly descriptive and code-grounded. The strictest supported judgment is that the current builder produces a mislabeled target object that is not a true residual; the replacement formula belongs in the next row, not hidden inside this one. | trust high | not ready | promise low | upside medium | risk not rescored | not fallback-worthy | — | This row is intentionally limited to the currently implemented defect. The replacement formula is a separate canonical claim so the reviewer does not have to disentangle “what code does now” from “what the archive recommends instead.” |
| The archive’s retained repair for `safety_residual` is a signed residual correction (`exact_safety - public_score`), not another clipped conservative-bound slack. | targets | `answer_18_combined.md:1320-1399`<br>`answer_18_combined.md:1848-1857` | “Use `Δ_s(a)` as the supervised target.”<br>“the model learns how much the public score should move” | partially supported | Repo support is only partial because current code provides the exact place where this repair would land—the existing replay builder, target slot, mask path, and loss path—but the repaired formula itself is not implemented. The repo supports the seam, not the repaired semantics. | docs absent | Current docs do not explicitly adopt this signed-residual formula. At most they support the broader idea that advanced targets need semantically credible construction before activation. | not in code | The repaired target is not in code now at all; the current builder still emits the older clipped target, so this row describes a proposal with a live insertion seam rather than any currently shipped behavior. | independently reproduced | Reconstructed from the archive’s explicit replacement formula and then checked against current code to verify that the target-builder seam, batching path, and loss surface already exist for this exact family. | Pass 1: the archive gives a concrete replacement formula, not just a complaint, and the formula is specific enough to be audited as its own canonical row. | Pass 2 + 3: keep this row proposal-only; the evidence supports “specific proposed repair with a live seam,” but not “repo-supported truth” or “docs-endorsed doctrine.” | trust medium | ready later | promise high | upside medium | risk not rescored | fallback worthy | fallback if search-derived safety labels stay unsafe | Split from the defect row so a reviewer can separately judge whether the current code is wrong and whether the archive’s replacement is actually worth adopting. |
| After stricter archive follow-ups, `safety_residual` is the only advanced target family still left near-term activatable; earlier broader “activate several advanced targets now” language does not survive. | targets | `answer_2-1_combined.md:166-180`<br>`answer_9_combined.md:1095-1101`<br>`answer_15_combined.md:462-512`<br>`answer_18_combined.md:1288-1290` | “The three active targets in this tranche”<br>“Ship exactly three live advanced targets”<br>“Activate only `safety_residual_target`, and activate it narrowly” | partially supported | Repo support here is comparative rather than absolute. The current repo makes the later-archive narrowing plausible because `safety_residual` already has a replay builder, mask, batch slot, head, and loss path, while ExIt still lacks a produced `exit_target` carrier, `delta_q` still lacks one semantically aligned normal training path, `belief_fields` and `mixture_weight` still rely on weaker Stage-A teacher semantics, and `opponent_hand_type` still lacks ontology/mapping/builder closure. | docs indirect | Current docs support supervision-first closure and repeatedly note that several advanced lanes still lack production paths or semantically credible teachers. They support a narrower `safety_residual`-first reading better than a broad simultaneous activation story, but they do not themselves prove strict exclusivity in one sentence. | partly in code | This row is only partly in code because the repo shows one relatively more-closed target family next to several visibly less-closed alternatives; it does not encode an explicit policy statement saying only `safety_residual` may be activated next. The code supports a comparative narrowing, not a hard doctrinal exclusivity rule. | independently reproduced | Reconstructed by comparing the early broad tranche rows with the later narrowing rows and then checking each competing target family against current repo closure criteria: produced carrier, semantically credible teacher, aligned target shape, and populated normal training path. | Pass 1: the archive really does contradict itself here, with earlier files proposing a broader near-term tranche and later files narrowing it sharply. | Pass 2 + 3: keep the claim comparative, not absolute. The strongest visible reading is that later archive passes narrow toward a `safety_residual`-first posture because the alternatives each fail a named closure criterion; the row should not be read as proof that no other future lane could ever qualify. | trust medium | ready later | promise high | upside medium | risk not rescored | fallback worthy | fallback if search-label closure stays weak | The row is intentionally comparative. It says later archive passes leave `safety_residual` closest to near-term activation; it does not claim that the current `safety_residual` object is already semantically good enough to turn on unchanged. |
| The current Stage-A `belief_fields` teacher is a public projection with equalized hidden-zone totals and a uniform kernel, not a credible public posterior teacher. | belief | `answer_2_combined.md:217-247`<br>`answer_3-1_combined.md:233-237`<br>`answer_15_combined.md:213-316`<br>`answer_18_combined.md:1473-1568` | “these heads should learn the information state, not the hidden state”<br>“The right supervised object is the projected belief itself”<br>“This target is not a posterior over hidden state.” | supported in repo | Repo support is direct because the current Stage-A teacher code actually constructs the criticized object: it projects belief mass into visible tables, equalizes hidden-zone totals, applies a uniform kernel, and then emits that product as the train-time teacher. The row is therefore supported by what the code literally builds, not by a speculative interpretation. | docs explicit | Current docs explicitly back the same doctrinal distinction: belief supervision should target projected/public belief objects rather than raw hidden-state allocation or arbitrary heuristic field values. The docs therefore support the diagnosis that the current Stage-A teacher is not the final intended posterior object. | partly in code | The path is `partial` in code because the target family and batching route are live, but what they currently carry is the criticized Stage-A projection teacher rather than a semantically stronger public posterior teacher. | independently reproduced | Reproduced by reconstructing the archive’s criticism from current `teacher/belief.rs` behavior and then cross-checking that the target really is wired through the existing batch/loss path. | Pass 1: current code and archive language line up on the central point that Stage-A emits a projected belief artifact, not a validated posterior teacher object. | Pass 2 + 3: keep the descriptive diagnosis separate from the activation recommendation, and state the concrete reason for `partial`: the path exists, but the teacher semantics are the weaker Stage-A version. | trust high | not ready | promise low | upside low | risk not rescored | not fallback-worthy | — | This row is about what the current teacher object is. The policy judgment “therefore keep it off” is intentionally in the next row so the reviewer does not have to infer causal structure from compressed wording. |
| Because the current Stage-A `belief_fields` teacher is only a public projection, the archive keeps `belief_fields` supervision off rather than treating it as a credible active-path target. | belief | `answer_15_combined.md:281-316`<br>`answer_18_combined.md:1559-1568` | “keep `w_belief_fields = 0.0`”<br>“`belief_fields`: semantically wrong, not privileged, off” | supported in repo | Repo supports the recommendation in the limited but concrete sense that the target family already exists and is wired, while the current teacher semantics remain the weaker projection-only version described in the previous row. The recommendation is therefore grounded in a visible mismatch between available plumbing and inadequate teacher semantics. | docs explicit | Current docs support this keep-off posture because they explicitly prefer semantically credible public-teacher objects and conservative staged activation over turning on weakly defined belief supervision early. | partly in code | The code state is `partial`: the target family is present and can be carried through training machinery, but it stays default-off and semantically untrusted because the teacher object has not met the stronger posterior-teacher standard. | independently reproduced | Reproduced by combining the archive’s explicit keep-off language with the current code fact that the target path exists but is still carrying the criticized Stage-A teacher object. | Pass 1: the archive clearly recommends keeping this target off until the teacher object is more credible than the current Stage-A projection. | Pass 2 + 3: keep the recommendation row explicit and self-sufficient; the visible reason for “off” is not mere absence, but the mismatch between live plumbing and weak current teacher semantics. | trust high | not ready | promise low | upside low | risk not rescored | not fallback-worthy | — | This row exists so the reviewer does not have to infer the policy consequence from the descriptive semantic row above. The answer is visible here: the path exists, but the archive still says to keep it off because the teacher object is not yet good enough. |
| The current Stage-A `mixture_weight` path inherits the same projection-teacher problem as `belief_fields` and also lacks a canonical component-identity contract. | belief | `answer_2_combined.md:217-247`<br>`answer_15_combined.md:295-316`<br>`answer_18_combined.md:1572-1600` | “Because component order is arbitrary”<br>“it inherits the belief failure and adds label-switching risk”<br>“`mixture_weight`: semantically wrong” | partially supported | Repo directly supports only two hard facts: Stage-A `mixture_weights` come from the same projection teacher as `belief_fields`, and no canonical component-ID contract is documented in code. The stronger label-switching diagnosis is an inference from that missing canonicalization. | docs indirect | Current docs support projected/public belief doctrine but do not define stable mixture component identities. | partly in code | Typed surface exists, Stage-A can emit weights, and code sorts mixture components at runtime, but no cross-sample canonical component contract closes the semantics. | independently reproduced | Reconstructed from archive lines and then cross-checked against current Stage-A belief construction to verify two distinct facts locally: the teacher is built from the same projection pipeline as `belief_fields`, and current code still does not define a stable cross-sample component-identity contract for mixture components. | Pass 1: the row survives because current code really does combine two visible issues in the same target family: projection-teacher inheritance from `belief_fields` and missing canonical component identity across samples. | Pass 2 narrows the wording to “lacks a canonical identity contract” instead of overstating instability as a proven theorem. | trust medium | not ready | promise low | upside medium | risk not rescored | not fallback-worthy | — | Descriptive row only. The keep-off policy is split into the next row so a reviewer can judge the semantic defect separately from the activation consequence. |
| Because the current `mixture_weight` teacher is projection-only and component identity is not canonically closed, the archive keeps `mixture_weight` supervision off. | belief | `answer_15_combined.md:295-316`<br>`answer_18_combined.md:1594-1600` | “keep `w_mixture_weight = 0.0`”<br>“`mixture_weight`: semantically wrong, not privileged, off” | partially supported | Repo supports the archive’s keep-off conclusion, but the strongest component-identity critique remains inference from missing canonicalization rather than explicit repo doctrine. | docs indirect | Docs support caution and projected/public teacher semantics, but do not explicitly discuss mixture-weight activation policy in this exact language. | partly in code | The path exists in code only in the narrow sense that Stage-A can emit mixture weights and the training surface can carry them, but it remains default-off and semantically unclosed because no canonical component-identity contract makes those weights stable enough for activation-quality supervision. | independently reproduced | Reconstructed from archive keep-off language and then checked against current code to verify that the target surface exists, the weights come from the same Stage-A projection teacher, and no stronger identity contract closes the path. | Pass 1: the archive does more than describe a semantic concern here; it makes an explicit keep-off recommendation tied to the current projection-only teacher and the still-open component-identity problem. | Pass 2 + 3: keep this as a recommendation row only. The strictest visible wording is that the target path exists but should remain off because the current teacher and identity contract are still too weak for activation. | trust medium | not ready | promise low | upside medium | risk not rescored | not fallback-worthy | — | Recommendation row only. This row answers the activation question raised by the prior descriptive row instead of forcing the reviewer to infer policy from the defect description. |
| The current dense `[B,46]` `delta_q` training path is not closed: the model head is `[46]`, the runtime/search feature seam is discard-level `[34]`, and the normal training path still has no populated builder or normal-batch target. | targets | `answer_2-1_combined.md:223-227`<br>`answer_7_combined.md:295-298`<br>`answer_7-1_combined.md:339-357`<br>`answer_15_combined.md:318-423`<br>`answer_16-1_combined.md:344-346`<br>`answer_18_combined.md:1604-1645` | “with an action mask keeping only actions with meaningful search support”<br>“`sample.rs` still leaves `delta_q_target: None`”<br>“runtime `delta_q` is `[34]`, not `[46]`” | supported in repo | Repo support is direct and multi-part: the model exposes a dense `[46]` `delta_q` head, `HydraTargets` exposes a dense slot, normal sample conversion still leaves that target absent, and bridge/runtime code exports only tile/discard-level `[34]` values. The row is therefore supported not by one missing piece but by a visible mismatch across head shape, runtime feature semantics, and sample-generation state. | docs indirect | Current docs explicitly say `delta_q_target` lacks a production path and that search-derived labels must be provenance-explicit. They only partially support the full row because the concrete `[46]`-versus-`[34]` mismatch is mainly a code-level finding rather than a docs-level explanation. | partly in code | `partial` here means two different surfaces exist today—a model-side training surface and a runtime/search feature surface—but the repo does not yet connect them through one semantically aligned, masked, end-to-end normal training path. | independently reproduced | Reconstructed by checking archive language against current `model.rs`, `sample.rs`, `losses.rs`, and `bridge.rs`, then confirming that the runtime and training surfaces called “delta_q” are not the same object in shape or provenance. | Pass 1: the row is supported because the repo really does expose both a dense training head and a narrower runtime/search feature seam under the same family name. | Pass 2 + 3: keep the wording concrete and self-sufficient—`delta_q` is neither wholly absent nor credibly closed; it is a mismatched set of surfaces that do not yet form one valid normal training path. | trust high | ready blocked | promise medium | upside medium | risk not rescored | fallback worthy | fallback if future search labels are chosen-action-only | This row intentionally says what “not closed” means: a head exists, a runtime feature exists, but the builder/provenance/masking story that would make them one trainable target family is still missing. |
| `opponent_hand_type` is a typed hole: the head exists, but the normal training path still has no ontology, no mapping spec, and no populated target builder. | targets | `answer_2_combined.md:258-277`<br>`answer_15_combined.md:320-359`<br>`answer_18_combined.md:1649-1675`<br>`answer_13_combined.md:1428-1431` | “Use the archetype set already implied”<br>“no taxonomy, no mapping”<br>“Stop spending attention on it until labels exist.” | supported in repo | Repo evidence is direct: the output surface exists, `HydraTargets` has the slot, normal sample conversion always leaves it `None`, and no ontology/mapping/builder is present in current code. | docs indirect | Docs keep richer opponent modeling on the reserve shelf, but they do not define the ontology or mapping needed to make this target active-path ready. | partly in code | Only the typed output surface and surrounding target slot exist today. There is still no ontology-backed target object, no mapping specification, and no populated normal training path that would let this head train on a semantically defined label family. | independently reproduced | Reconstructed from archive lines and then checked against current `model.rs` and `sample.rs` behavior to verify that the head surface exists while the ontology, mapping, and normal target builder are all still absent. | Pass 1: the row survives because the repo clearly distinguishes between “head exists” and “target family is semantically closed,” and `opponent_hand_type` only satisfies the first half of that pair. | Pass 2 keeps the descriptive typed-hole claim separate from the keep-off recommendation below. | trust high | ready blocked | promise low | upside low | risk not rescored | not fallback-worthy | — | Descriptive row only. The separate keep-off row exists so the typed-hole diagnosis here does not have to carry the policy recommendation by implication. |
| Because `opponent_hand_type` lacks an ontology-backed builder and populated target path, the archive keeps it off rather than activating it on weak labels. | targets | `answer_15_combined.md:350-359`<br>`answer_18_combined.md:1669-1675`<br>`answer_13_combined.md:1428-1431` | “Do not populate it until a class ontology and mapping spec exist.”<br>“`opponent_hand_type`: typed hole … off” | supported in repo | Repo supports the keep-off conclusion because the head surface exists without the ontology/builder chain needed to make the target meaningful. | docs indirect | Docs imply reserve-shelf timing and later richer opponent modeling, but do not provide activation criteria beyond the missing ontology. | partly in code | The path is not merely disabled by policy; it is practically off because current code still has no ontology-backed target object or populated builder that could feed this head with usable labels during normal training. | independently reproduced | Reconstructed from archive keep-off language plus the current missing ontology/builder chain. | Pass 1: the archive’s recommendation survives because current repo surfaces show a head without the ontology and builder chain needed to make activation anything other than weak-label guesswork. | Pass 2 + 3: keep this row explicitly normative. The strictest visible reading is not just “target absent,” but “do not activate the head on underspecified labels while the ontology/builder gap remains open.” | trust high | not ready | promise low | upside low | risk not rescored | not fallback-worthy | — | Recommendation row only. It is separate from the typed-hole description so the reviewer can see both the structural defect and the resulting keep-off decision without collapsing them. |
| `oracle_critic` is currently a detached optional privileged branch: the target path exists, the head is live, and the oracle branch is fed from a detached pooled representation. | targets | `answer_2_combined.md:188-216`<br>`answer_15_combined.md:498-512`<br>`answer_18_combined.md:1679-1704`<br>`answer_13_combined.md:1411-1414` | “Used only for `oracle_critic`”<br>“Keep `oracle_target` / oracle critic on the privileged side only”<br>“the oracle head currently cannot shape the shared representation” | partially supported | Repo code proves three narrower facts: oracle target plumbing exists, the head is live, and the oracle path is detached and default-optional. Repo does not add a stronger enforcement boundary beyond that. | docs indirect | Current docs explicitly warn against expanding privileged pathways before public-target closure and note the detached oracle path. | partly in code | The detached oracle branch is implemented as a real auxiliary path in code, but it remains outside the student-closure story because it is optional, privileged, and prevented from shaping shared representation through the detached pooled input. | independently reproduced | Reconstructed from archive lines and then checked against current model/loss code to confirm the exact combination of facts the row claims: head exists, target plumbing exists, branch is optional, and oracle input is detached from the shared pooled student representation. | Pass 1: the row survives as a descriptive state claim because the repo really does implement a detached privileged auxiliary branch rather than merely gesturing at one in docs. | Pass 2 + 3: keep the wording descriptive and role-focused. The strictest visible claim is not “oracle path solves anything,” but “oracle branch exists as a detached optional auxiliary mechanism with limited scope.” | trust medium | not ready | promise low | upside low | risk not rescored | not fallback-worthy | — | Descriptive row only. A separate interpretive row follows so the reviewer does not have to infer from this state description whether the branch should count toward student-target closure. |
| The archive explicitly treats `oracle_critic` as non-closure for the student path: it should not be counted as evidence that advanced public/student targets are already solved. | targets | `answer_15_combined.md:503-512`<br>`answer_18_combined.md:1697-1704` | “do not count it as tranche acceptance”<br>“privileged-only diagnostic” | partially supported | Repo supports the warning indirectly: the branch is detached and optional, so it cannot by itself prove that student-facing target closure exists. | docs indirect | Current docs support the same sequencing rule: privileged guidance later, public-target closure first. | not in code | No code path turns oracle presence into student-target closure; that would be an interpretation error, not an implementation state. | independently reproduced | Reconstructed from archive sequencing language and then checked against current detached-branch behavior to verify that nothing in code turns the oracle path into evidence of closed public/student supervision. | Pass 1: the warning survives because the row is responding to a real interpretive risk—mistaking a detached privileged auxiliary branch for proof that student-facing target closure is already solved. | Pass 2 + 3: keep the row explicitly interpretive and sequencing-focused. The strongest visible wording is that current oracle-branch existence does not justify counting public-target closure as solved. | trust medium | not ready | promise low | upside low | risk not rescored | not fallback-worthy | — | Recommendation/interpretation row only. It stays separate from the descriptive oracle-branch row so the reader can distinguish “what exists” from “how not to overread what exists.” |
| Conservative ExIt remains doctrinally alive in the archive, but only in a later masked policy-head reuse form rather than as broad immediate search imitation. | search | `answer_2-1_combined.md:209-221`<br>`answer_7-1_combined.md:312-357`<br>`answer_9_combined.md:1232-1238`<br>`answer_9_combined.md:1280-1325`<br>`answer_15_combined.md:479-483` | “ExIt target”<br>“planner improves policy; apprentice imitates planner”<br>“masked, partial-policy distillation” | partially supported | Repo supports the narrowed doctrinal claim: ExIt remains central in docs and loss helper logic, but current active-path closure is still later and masked rather than broad immediate use. | docs explicit | Current docs keep ExIt doctrinally central while also saying its production path is not yet closed, so docs support the direction of the row as doctrine but not as an already-shipped training path. | not in code | This row is not about a live code path. It records that ExIt remains a documented/archive direction even though the masked policy-head reuse form it describes still awaits a real upstream carrier and batch path. | independently reproduced | Reconstructed from archive doctrine rows and current ExIt-oriented docs together, so the claim does not depend on archive language alone; it survives because both sources keep ExIt alive as a direction while deferring full path closure. | Pass 1: the row survives because the archive and current docs both keep conservative ExIt alive as doctrine even while denying that its data path is already complete. | Pass 2 + 3: keep doctrine separate from implementation. The strictest visible wording is “documented/archive direction with a narrowed later form,” not “active training path” and not “fully repo-supported mechanism.” | trust medium | ready later | promise medium | upside medium | risk not rescored | fallback worthy | fallback if explicit search-policy batches are added later | Doctrine row only. The separate carrier row below answers the concrete implementation question so this row can stay focused on doctrine rather than plumbing. |
| Current Hydra still lacks an evidenced end-to-end `exit_target` carrier: helper/loss-side support exists, but the normal sample and batch path do not yet produce a real upstream `exit_target`. | search | `answer_2-1_combined.md:205-207`<br>`answer_2-1_combined.md:279-286`<br>`answer_15_combined.md:479-483` | “add exactly one new training capability: optional advanced-target plumbing”<br>“Produced: `exit_target`”<br>“`exit_target` is not actually carried anywhere” | partially supported | Repo evidence is concrete: loss-side helper support exists and docs expect the path, but current normal sample/batch production still does not populate a real upstream `exit_target` carrier. | docs explicit | Reconciliation explicitly says upstream `exit_target` production still needs to become real, which makes docs support stronger here than in most proposal rows: the missing carrier is not just an archive complaint but a live documented gap. | partly in code | The path is only partly present because loss-side consumption helpers exist, but the normal sample and batch pipeline still do not produce a genuine upstream `exit_target` object that would let the doctrine become a full training path. | independently reproduced | Reconstructed from archive lines and then checked against current helper/loss code plus the still-missing sample/batch production path, so the row rests on visible carrier absence rather than on vague ExIt skepticism. | Pass 1: the row survives because current repo/docs expose a very specific mismatch—consumer-side helper support exists while producer-side carrier generation is still absent. | Pass 2 keeps this descriptive repo-state row separate from the ExIt doctrine row. | trust medium | ready blocked | promise medium | upside medium | risk not rescored | fallback worthy | fallback if search-policy targets are added before broader search | Descriptive repo-state row only. This row exists so the doctrinal ExIt row above does not have to carry detailed plumbing claims that belong in a separate carrier-closure judgment. |
| Current Hand-EV field names overclaim exactness: `tenpai_prob`, `win_prob`, and especially `expected_score` name stronger semantics than the live heuristic formulas actually provide. | hand_ev | `answer_17_combined.md:1163-1370`<br>`answer_14_combined.md:241-307` | “This is not semantically ‘probability of reaching tenpai…’”<br>“It is not a clean probability of winning.”<br>“The score field is unsalvageable as a live semantic claim.” | supported in repo | Repo directly implements the criticized formulas: heuristic continuation boosts, fallback base-win logic, and `expected_score = win_prob[2] * conditional_score_estimate(...)`. | docs indirect | Reconciliation says current Hand-EV win/score modeling remains heuristic; docs name the channels but do not validate exact probabilistic semantics. | fully in code | The named Hand-EV channels are fully live in runtime feature computation today, but the formulas behind them are heuristic enough that the names overstate what the values actually mean when interpreted as exact probabilities or exact expected score. | independently reproduced | Reconstructed from archive criticism and then verified directly against current `hand_ev.rs` formulas so the row rests on visible naming-versus-formula mismatch rather than on a generic distrust of heuristics. | Pass 1: the row survives because current code really does expose the named channels while implementing formulas that are weaker and more heuristic than the names suggest. | Pass 2 keeps the wording narrower than “all Hand-EV is fake”; the problem is semantic overclaim in the named outputs. | trust high | not ready | promise medium | upside medium | risk not rescored | not fallback-worthy | — | Descriptive semantic row only. The row is about naming/semantic overclaim in a live feature family, not about removing Hand-EV entirely or denying that the signals are useful. |
| H1a exact one-step Hand-EV semantic repair is the archive’s leading immediate Hand-EV proposal. | hand_ev | `answer_14_combined.md:181-199`<br>`answer_14_combined.md:308-367`<br>`answer_17_combined.md:1300-1345` | “This is the strongest immediate Hand-EV upgrade”<br>“Try first: H1a exact one-step Hand-EV semantic repair” | partially supported | Repo support is partial because the current codebase already contains the interface, insertion seam, and project sequencing that make H1a a plausible immediate proposal, but it does not implement the repaired semantics or bless them as current doctrine. | docs indirect | Current docs support Hand-EV realism before deeper AFBS work, which is enough to support H1a as a sequencing-compatible proposal, but they still stop short of endorsing the exact one-step formula set described in the archive. | not in code | The proposal is not in code now. What exists in code is only the live Hand-EV interface and insertion seam that make this proposal implementable without architectural churn. | independently reproduced | Reconstructed from archive proposal rows and then checked against the current Hand-EV surface so the row rests on both the proposal wording and the visible seam that would host it. | Pass 1: the row survives because the archive clearly elevates H1a above other immediate Hand-EV options rather than merely mentioning it as one candidate among many. | Pass 2 + 3: keep it proposal-only and ranking-focused. The strictest visible wording is “explicitly favored immediate archive proposal with a live seam,” not “repo-endorsed fix” and not “implemented near-term plan.” | trust medium | ready later | promise high | upside medium | risk not rescored | fallback worthy | fallback if deeper search stays deferred | Proposal ranking row only. The insertion-seam row below carries the concrete interface claim so this row can stay focused on proposal priority. |
| The retained H1a Hand-EV repair proposal reuses the current `HandEvFeatures` / 42-plane interface and existing bridge-to-encoder insertion seam rather than requiring new heads. | hand_ev | `answer_14_combined.md:194-199`<br>`answer_14_combined.md:308-367` | “do not change the tensor interface”<br>“keep the current 42-channel layout” | supported in repo | Repo directly supports this seam claim because the current Hand-EV payload already flows through `HandEvFeatures`, bridge code, and encoder channels. | docs indirect | Current docs support Hand-EV realism earlier than deeper search, but the seam detail is more explicit in the archive than in docs. | partly in code | The current 42-plane interface, `HandEvFeatures` payload, bridge path, and encoder ingestion are all live in code. What is missing is not the surface, but the repaired semantics that the archive wants to feed through that already-live surface. | independently reproduced | Reconstructed from archive seam language and then verified against current `hand_ev.rs`, bridge, and encoder flow so the row is anchored in one concrete insertion seam rather than a vague “should fit somewhere” claim. | Pass 1: the row survives because the archive does more than rank H1a; it specifies that the repair should reuse the existing interface rather than expand the model surface. | Pass 2 keeps the seam claim separate from the proposal-ranking claim above. | trust high | ready later | promise medium | upside low | risk not rescored | fallback worthy | fallback if feature-surface churn is undesirable | Concrete insertion-seam row only. It complements the proposal-ranking row above by answering the implementation-surface question directly: same interface, same bridge path, no new heads. |
| A real runtime seam already exists for world-aware CT-SMC Hand-EV: bridge/runtime code can condition Group-D-style Hand-EV features on `SearchContext.ct_smc`. | hand_ev | `answer_1-1_combined.md:341-350`<br>`answer_14_combined.md:200-216`<br>`answer_14_combined.md:369-427` | “Do not compute Hand-EV from one marginal count vector if CT-SMC exists.”<br>“SearchContext.ct_smc exists” | supported in repo | Repo directly supports the runtime seam: current bridge logic dispatches to CT-SMC-aware Hand-EV when CT-SMC context is present. | docs indirect | Docs imply CT-SMC as search-grade posterior and later selective improvements, but do not foreground this seam as active doctrine. | partly in code | The runtime seam is real and visible in current code: CT-SMC-aware Hand-EV can already be invoked when `SearchContext.ct_smc` is present. The row is only partly in code because that runtime seam still lacks closed train/infer parity. | independently reproduced | Reconstructed from archive seam rows and then checked against current bridge/runtime dispatch so the row is grounded in actual CT-SMC-conditioned Hand-EV invocation rather than just conceptual compatibility. | Pass 1: the row survives because there is a concrete runtime seam, not just a hypothetical place where world-aware Hand-EV might someday fit. | Pass 2 keeps this descriptive seam row separate from the parity blocker and second-stage policy rows below. | trust high | ready later | promise medium | upside low | risk not rescored | fallback worthy | fallback if public-count Hand-EV realism saturates | Descriptive seam row only. The parity blocker and second-stage staging rows follow separately so this row can stay focused on the runtime fact that the seam already exists. |
| Train/infer parity for world-aware CT-SMC Hand-EV is not closed: the current training path does not reconstruct matching search-context observations for standard model-input use. | hand_ev | `answer_14_combined.md:453-454`<br>`answer_14_combined.md:502-559` | “loader does not build search-context observations”<br>“[blocked] for immediate mainline” | supported in repo | Repo evidence directly supports the blocker: runtime can condition on CT-SMC context, but the standard training pipeline does not build matching search-context observations. | docs indirect | Docs imply the same blocker indirectly by keeping this lane later and specialist, but the parity problem is more explicit in the archive than in docs. | partly in code | The runtime side can already consume CT-SMC context, but the standard training side still does not reconstruct matching search-context observations. The row is therefore about a concrete train/infer asymmetry, not a vague “not ready yet” claim. | independently reproduced | Reconstructed from archive blocker lines and then checked against current loader/runtime surfaces to verify that the missing train-side reconstruction is the real blocker, not just general project caution. | Pass 1: the row survives because current code exposes a concrete asymmetry—runtime seam present, training reconstruction absent—that directly explains why mainline parity is still open. | Pass 2 keeps the blocker separate from the second-stage recommendation below. | trust high | ready blocked | promise medium | upside medium | risk not rescored | not fallback-worthy | — | Descriptive blocker row only. The staging row below uses this blocker as one reason for delaying promotion, but the blocker itself is explicit and independently auditable here. |
| Because train/infer parity is not closed and representative-world compression must clear its own gates, the archive keeps world-aware CT-SMC Hand-EV as second-stage only. | hand_ev | `answer_14_combined.md:612-616` | “Try second, only if H1a wins and CT-SMC gates pass” | partially supported | Repo supports this row only indirectly through the already-established seam and blocker: runtime can host the idea, but current training does not reconstruct matching search-context observations and current repo surfaces do not close the representative-world promotion gates. Repo therefore supports the prerequisites for a later lane, not present promotion. | docs indirect | Current docs support later selective improvement after supervision closure and simpler Hand-EV realism work, but they do not literally name this exact world-aware lane as active doctrine. The docs are consistent with second-stage status rather than explicit proof of it. | not in code | No code path marks this lane as active-path behavior or default runtime/training policy. What exists in code today is only the runtime seam plus the parity blocker, not a promoted second-stage mechanism. | independently reproduced | Reconstructed by combining the archive’s explicit staging quote with the current repo facts already established in this table: a real runtime seam exists, train-side parity does not, and no representative-world promotion path is implemented. | Pass 1: the archive explicitly stages this lane after H1a and after additional CT-SMC gates, so the row survives as a sequencing claim rather than a live-feature claim. | Pass 2 + 3: keep the row as a staging judgment only. The strictest visible wording is that current repo evidence permits a later lane but does not justify immediate promotion because both parity and representative-world gate closure are still missing. | trust medium | ready later | promise high | upside medium | risk not rescored | fallback worthy | fallback if public-count Hand-EV realism underdelivers | This row depends on the seam/blocker pair established above: runtime CT-SMC Hand-EV is possible, but current training parity and representative-world promotion conditions are still open. The row answers the staging question, not the implementation question. |
| Current endgame code is a selective particle-weighted shell, not a true exact multiplayer endgame solver. | runtime | `answer_14_combined.md:221-235`<br>`answer_14_combined.md:548-559` | “The shell already exists”<br>“full multiplayer exact endgame solving ... does not survive” | supported in repo | Repo strongly supports this description: current endgame code gates on wall/threat and aggregates `eval_fn` over selected particles. | docs explicit | Reconciliation places exactification and endgame work on the reserve shelf after supervision and feature-realism work, which supports the row’s distinction between a live shell and a non-live exact multiplayer solver more directly than a generic “later” note would. | partly in code | Current code really does implement a specialist particle-weighted shell, including gating and particle aggregation, but it does not implement a full exact multiplayer solver with the stronger semantics the archive rejects here. | independently reproduced | Reconstructed from archive lines and directly checked against `endgame.rs` so the row rests on a concrete shell-versus-exactness distinction visible in code. | Pass 1: the row survives because the code already contains something real in this area—a selective shell—while the archive and docs both reject overstating that shell as exact multiplayer solving. | Pass 2 keeps this descriptive code-state claim separate from the later specialist recommendation below. | trust high | not ready | promise low | upside low | risk not rescored | not fallback-worthy | — | Descriptive code-state row only. The later specialist-leaf row below answers the promotion question; this row only states what current code actually is. |
| Because train/infer parity is not closed and representative-world validation is still open, the archive keeps world-aware CT-SMC Hand-EV as second-stage only. | runtime | `answer_3-1_combined.md:250-256`<br>`answer_14_combined.md:600-616` | “keep, phase-next #1”<br>“Endgame exactification is later and narrower” | partially supported | Repo supports this row only indirectly through the already-established seam and blocker: runtime can host the idea, but current training does not reconstruct matching search-context observations and current repo surfaces do not yet prove that any representative-world approximation preserves action-relevant quality under equal-budget comparison. Repo therefore supports prerequisites for a later lane, not present promotion. | docs explicit | Current docs support later selective improvement after supervision closure and simpler Hand-EV realism work, but they do not literally name this exact world-aware lane as active doctrine. The docs are consistent with second-stage status rather than explicit proof of it. | not in code | The stronger exact leaf is not in code now. What is live is only the host shell that could call such a leaf later, so this row is a recommendation/staging claim rather than an implementation claim. | independently reproduced | Reconstructed from archive staging language and then checked against current `endgame.rs` shell behavior to confirm that the row is about a later leaf inside an existing host, not about a currently shipped exact solver. | Pass 1: the archive keeps exactification alive only in a narrower, later specialist form. | Pass 2 + 3: keep the wording explicit that the shell exists now but the stronger exactifying leaf does not. The row should read as “later specialist extension to a live host shell,” not “current endgame identity.” | trust medium | ready later | promise medium | upside low | risk not rescored | fallback worthy | fallback if late-game specialist value is proven offline | Sequencing row only. The visible prerequisites are now explicit inside the row itself: train/infer parity must close, and any representative-world approximation must clear its own validation gates before promotion. |
| A tiny learned scorer for the next ponder quantum is a narrow surviving compute-allocation idea retained across the archive. | search | `answer_5-1_combined.md:402-406`<br>`answer_10_combined.md:205-217`<br>`answer_19_combined.md:1631-1660` | “replace the current heuristic hard-state trigger”<br>“Replace the scalar heuristic with a tiny cost-sensitive scorer”<br>“Learn exactly one thing” | partially supported | Repo supports a precise insertion seam: queue ordering is already scalar and local, but the learned scorer and its offline labels do not exist yet. | docs indirect | Current docs support selective compute and pondering importance, but they do not promote a learned tiny scorer into active doctrine. | not in code | Current code still uses the hand-written ponder-priority heuristic. The row is therefore not claiming any learned scorer is partly shipped; it is claiming that the surviving idea has a visible compute-allocation seam but no implemented model or training labels yet. | independently reproduced | Reconstructed from archive proposal rows and then checked against the current queue and scheduler surfaces so the row is grounded in one real surviving idea rather than a generic “maybe learning could help” guess. | Pass 1: the row survives because the archive repeatedly narrows broader routing ideas down to this much smaller learned-scorer proposal instead of abandoning compute learning entirely. | Pass 2 keeps the idea row separate from the replacement-target seam below. | trust medium | ready later | promise medium | upside low | risk not rescored | fallback worthy | fallback if broad routing stays unjustified | Proposal-identity row only. The next row carries the exact seam—replace the scalar queue heuristic—so this row can stay focused on which compute-learning idea actually survives the archive narrowing. |
| The intended seam for that tiny scorer is replacement of the current queue-priority heuristic, not introduction of a full runtime router or learned halting policy. | search | `answer_10_combined.md:213-217`<br>`answer_19_combined.md:1635-1645`<br>`answer_19_combined.md:1909-1927` | “Replace the scalar heuristic”<br>“Do **not** learn ... a full runtime router”<br>“Use a sign-constrained linear model.” | supported in repo | Repo directly supports the seam: `compute_ponder_priority` already exists as a scalar heuristic, and `PonderTask.priority_score` is the current insertion point. | docs indirect | Docs describe the seam indirectly through pondering doctrine and hard-state compute, but the “replace exactly this heuristic” framing is archive-specific. | partly in code | The seam is live because current code already computes a scalar queue-priority value and stores it on ponder tasks. What is not live is the learned model that would replace that heuristic without expanding into a broader routing regime. | independently reproduced | Reconstructed from archive seam language and then checked against `afbs.rs` queue surfaces so the row is anchored in one concrete replacement site rather than in a high-level “learn routing” slogan. | Pass 1: the row survives because the archive specifies a narrow replacement target—the current scalar queue heuristic—instead of vaguely gesturing at learned compute policy. | Pass 2 + 3: keep the row seam-specific. The strictest visible wording is “replace the current queue-priority heuristic here,” not “introduce a general learned runtime router.” | trust high | ready later | promise medium | upside low | risk not rescored | fallback worthy | fallback if queue ranking still matters after cache/provenance fixes | Concrete seam row only. It complements the prior proposal-identity row by saying exactly what existing heuristic the archive wants to replace and what broader routing idea it explicitly rejects. |
| A broad multi-arm learned router is blocked by current runtime reality: the cited live inference path is still basically cache hit or actor+SaF, not a rich menu of callable arms. | architecture | `answer_10_combined.md:197-201`<br>`answer_10_combined.md:233-245` | “full learned router ... is blocked by current runtime surfaces”<br>“Right now this would mostly be a paper design” | supported in repo | Repo strongly supports this blocking judgment because current inference exposes only cache-hit reuse or actor+SaF fast path for the cited live path. | docs explicit | Reconciliation explicitly rejects broad “search everywhere” rollout and keeps AFBS specialist and hard-state gated, which gives this negative row stronger docs support than a mere absence claim would have. | fully in code | This negative statement is fully borne out by current runtime code: the cited live path still resolves to cache-hit reuse or actor-plus-SaF rather than a dispatch layer over many callable runtime arms. | independently reproduced | Reconstructed from archive lines and directly checked against current inference/runtime behavior so the block rests on one visible runtime fact pattern rather than on abstract skepticism about routers. | Pass 1: the row survives because both docs and code agree that current Hydra does not expose the sort of rich multi-arm runtime surface a learned router would need to route over. | Pass 2 keeps the wording narrow to the cited current runtime surface. | trust high | not ready | promise low | upside low | risk not rescored | not fallback-worthy | — | Negative prioritization row only. The row says the current runtime surface is too narrow for a broad router to be a serious near-term path; it does not say routing is impossible forever. |
| Current cached ponder/rollout authority has weak semantic identity and provenance: cache entries are keyed too weakly and `PonderResult` lacks the provenance fields needed to justify live authoritative reuse. | rollout | `answer_13_combined.md:865-884`<br>`answer_16-1_combined.md:350-354`<br>`answer_20_combined.md:1030-1124`<br>`answer_20_combined.md:1126-1231` | “authority without semantic identity”<br>“no `source_net` / `source_version` on `PonderResult`”<br>“same observation hash ... does not [imply same semantic root]” | supported in repo | Repo support is strong because the criticized ingredients are all present in the current runtime path: cache lookup is keyed by a plain state hash, `PonderResult` carries only shallow summary fields such as policy/value/depth/visits/timestamp, and inference can early-return on a cache hit without provenance-rich identity or trust-class checks. | docs indirect | Current docs support the general doctrine that search-derived signals need provenance and that AFBS should remain specialist/gated, but they do not explicitly document the current cache-authority bug in the same concrete terms as the archive/code audit. | fully in code | `yes` here means the criticized behavior is happening now in live code: cache hits can directly influence action selection, while the identity fields that would justify that authority—source net, source version, belief/runtime cohort, trust class—are absent. | independently reproduced | Reconstructed from archive provenance criticism and then independently rechecked against the current cache/result/inference surfaces to confirm that the runtime really can reuse these weakly identified summaries as authoritative outputs. | Pass 1: the row survives because the provenance weakness is not a vague design concern; it is a visible property of the current cache key, result payload, and inference early-return path. | Pass 2 + 3: keep the row purely descriptive and explicit about what is missing, so the reviewer can see the exact basis for the “yes” and “in code now” judgments without needing the recommendation row below to interpret it. | trust high | ready now | promise medium | upside high | risk not rescored | not fallback-worthy | — | The key distinction is visible in this row: current runtime authority is real, and provenance weakness is real. The policy answer to that problem—“therefore keep these outputs learner-only”—is intentionally separated into the next row. |
| The archive’s safe-policy recommendation is to keep current ponder/rollout outputs learner-only and off the live decisive authority path until provenance and admission closure exist. | rollout | `answer_16-1_combined.md:367-379`<br>`answer_20_combined.md:1409-1549` | “Do **not** let RolloutNet participate in live decisive AFBS root computation”<br>“all current ponder/rollout outputs should remain learner-only” | not supported | Current repo does not implement this learner-only safety policy. The live runtime still permits the cache-hit authority behavior criticized in the preceding provenance row, so the row is supported only as an archive recommendation responding to a real runtime defect, not as a description of current repo policy. | docs indirect | Current docs partially support this direction because they favor conservative specialist AFBS, provenance-aware search labels, and a fast actor+SaF mainline, but they do not literally say that cached ponder/rollout outputs are already excluded from decisive authority use. | not in code | This policy is not in code now. Current runtime still allows authoritative reuse of cache-hit summaries, so the learner-only posture described here remains a recommended future regime rather than a shipped runtime rule. | independently reproduced | Reconstructed from explicit archive policy language and then checked against the current runtime path to verify that the recommendation is a proposed safety response to the provenance gap rather than an already-enforced code path. | Pass 1: the archive clearly gives a policy answer—keep rollout/ponder outputs learner-only until provenance and admission are closed—rather than merely criticizing current cache semantics. | Pass 2 + 3: keep the row explicitly normative. The strictest visible wording is “recommended later policy in response to a current provenance defect,” not “current Hydra doctrine” and not “current code behavior.” | trust medium | ready later | promise medium | upside high | risk not rescored | fallback worthy | fallback if provenance never closes | This row is intentionally paired with the descriptive provenance-defect row above. The earlier row says what is wrong in current code; this row says what policy the archive recommends because of that defect. |
| After stricter archive revalidation, the only exact rollout-admission threshold retained is `top2_policy_gap < 0.10`. | rollout | `answer_16-1_combined.md:336-345`<br>`answer_16-1_combined.md:389-415` | “Grounded hard-state minimum: `top2_policy_gap < 0.10`”<br>“These exact thresholds survive revalidation” | partially supported | Repo supports this row in a narrow way: `top2_policy_gap` is a surfaced runtime signal and the 0.10 breakpoint already appears in current scheduler logic, but repo does not support a broader exact rollout-admission pack around it. The support is for this one retained exact threshold, not for a full trust regime built from it. | docs indirect | Current docs discuss small top-2-gap hard states more concretely than other rollout-threshold candidates, but they still do not define a full rollout-admission policy from this number alone. The docs support retention of the threshold better than they support the larger policy interpretation. | partly in code | Code uses `top2_policy_gap` and hardcodes the 0.10-style breakpoint in scheduler logic, but it does not implement a full admission rule saying that this threshold alone authorizes rollout. So `partial` means “exact signal and breakpoint exist, full policy does not.” | independently reproduced | Reconstructed from the stricter archive revalidation and then checked against current runtime code to confirm that this is the one exact threshold still visibly surfaced, while the broader rollout-threshold bundle is not. | Pass 1: the stricter archive pass preserves `top2_policy_gap < 0.10` as the one exact retained threshold after demoting other rollout cutoffs. | Pass 2 + 3: keep the numeric claim narrow and local. The row should be read as “sole exact threshold retained from the cited rollout-threshold set,” not as proof that docs/code fully canonize a complete rollout-admission regime around that number. | trust medium | ready later | promise low | upside medium | risk not rescored | fallback worthy | fallback if rollout trust rules stay empirical | The critical visible distinction is that the threshold itself survives, while the wider exact threshold pack does not. This row is about the retained exact number, not about a full rollout policy already living in repo code. |
| Earlier exact rollout numeric thresholds such as `risk_score > 0.08` and `particle_ess < 0.55` are explicitly rejected or demoted by the stricter archive revalidation. | rollout | `answer_16_combined.md:168-177`<br>`answer_16_combined.md:214-227`<br>`answer_16-1_combined.md:417-440` | “`risk_score > 0.08` ... `[inference]`”<br>“I am **not** keeping the earlier exact `risk_score` and `particle_ess` cutoffs.”<br>“Rejected: `risk_score > 0.08` and `particle_ess < 0.55`” | supported in repo | Repo exposes the signals but does not support those earlier exact numeric cutoffs; the stricter archive follow-up explicitly removes them. | docs indirect | Docs describe risk and ESS qualitatively and do not canonize the specific rejected numbers from the earlier archive draft. That matters because the row is not rejecting the signals themselves; it is rejecting overprecise numeric thresholds that the stricter archive pass backed away from. | not in code | Those specific rejected thresholds are not codified in code. Current runtime may expose related signals, but the exact `risk_score > 0.08` and `particle_ess < 0.55` cutoffs are not live policy in the repo. | independently reproduced | Reconstructed from earlier archive draft vs stricter follow-up comparison plus current signal surfaces. | Pass 1: the row survives because there is a real archive contradiction here—an earlier draft proposes exact numeric cutoffs and the stricter follow-up explicitly rejects them. | Pass 2 keeps this as a rejected-precision cleanup row rather than letting the earlier numbers leak into current doctrine. | trust high | not ready | promise none | upside low | risk not rescored | not fallback-worthy | — | Reject/cleanup row. Its purpose is to stop superseded exact numbers from sneaking back in as if they were current doctrine; `answer_16-1` is the stricter canonical source over `answer_16`. |
| Action-sufficient CT-SMC world compression is retained only as a narrow benchmark-first project: a runtime-only, discard-phase, equal-budget falsification lane rather than a current active-path method. | fallback | `answer_6-1_combined.md:467-474`<br>`answer_8-2_combined.md:304-346`<br>`answer_11_combined.md:586-690` | “no v0 changes ... Runtime first; distillation / labels later”<br>“Action-sufficient CT-SMC world compression”<br>“worth it now only as a very narrow, runtime-only falsification project” | partially supported | Repo supports only the adjacent seams a benchmark would need: CT-SMC particles exist, Hand-EV and endgame already consume world-relevant structure, and current runtime/search code can host discard-phase experimentation. Repo does not implement an action-sufficient compression operator, train-side export, or active runtime policy for this family. | docs absent | Current docs do not elevate this lane into the active path. At most they leave room for it as a reserve/research candidate that could fit existing CT-SMC and specialist-runtime doctrine later if a narrow benchmark succeeds. | not in code | No compression mechanism is live in code now. What exists are only the neighboring runtime objects and evaluation seams that make a narrow falsification benchmark possible. | independently reproduced | Reconstructed from archive benchmark-first compression language and then cross-checked against current CT-SMC, Hand-EV, and endgame seams to confirm that the row is about a hostable experiment, not a partially implemented method. | Pass 1: the archive keeps one narrow survivor in this family, but only as a benchmark-first lane rather than a production or training method. | Pass 2 + 3: keep every qualifier visible. The strictest wording is “runtime-only, discard-phase, equal-budget falsification lane attached to real seams,” not “current method,” “active doctrine,” or “latent implementation.” | trust medium | ready later | promise medium | upside medium | risk not rescored | fallback worthy | fallback if runtime cost stays too high | The qualifiers matter and are not filler: runtime-only means no train-side export, discard-phase means the seam is narrow, equal-budget means benchmark honesty, and falsification lane means the project should die fast if it does not beat simpler baselines. |
| Posterior-consensus ExIt over hidden worlds is rejected as currently scoped because the required public world-conditioned action-teacher object is missing and the idea collapses into ordinary uncertainty-weighted ExIt when narrowed. | search | `answer_8-1_combined.md:306-342`<br>`answer_12_combined.md:137-151`<br>`answer_12_combined.md:173-214` | “Posterior-consensus ExIt/ΔQ distillation”<br>“Verdict: kill as currently scoped”<br>“it reduces to ordinary ExIt with confidence weighting” | repo-blocked | Repo lacks the decisive object, not just the plumbing: there is no evidenced public world-conditioned action teacher that would make this family semantically distinct from ordinary confidence-weighted ExIt. The blocker therefore sits at the level of teacher-object definition before any normal batch path or loss wiring question. | docs indirect | Current docs keep narrower ExIt and target-closure work alive, but they do not define the missing public world-conditioned teacher object this family would require. Doctrine therefore does not rescue the family in its current scoped form. | not in code | No such teacher object, carrier, or mechanism exists in current code. This is not a typed-but-inactive lane; it is a missing-object rejection. | independently reproduced | Reconstructed by checking the archive’s rejection rationale against current docs/code and confirming that no defensible public world-conditioned action-teacher object is evidenced anywhere in the current Hydra surfaces. | Pass 1: the archive rejects the family because its distinctive teacher object is missing, not merely because implementation work remains undone. | Pass 2 + 3: keep the rejection grounded on the strongest visible reason: without a public world-conditioned teacher object, the proposal either remains undefined or collapses into a much plainer uncertainty-weighted ExIt story. | trust medium | not ready | promise none | upside high | risk not rescored | not fallback-worthy | — | The rejection is semantic before it is infrastructural. That is why this row stays separate from the narrower ExIt doctrine/carrier rows: the problem here is not “path missing,” but “distinctive teacher object never closes.” |
| Regime-coupled opponent filtering / latent opponent posterior feedback is rejected as currently scoped because the required emission model, particle-side regime state, and downstream consumers are missing from the evidenced surfaces. | architecture | `answer_12_combined.md:153-166`<br>`answer_12_combined.md:173-214` | “Verdict: kill.”<br>“No such grounded module or label builder is evidenced.” | repo-blocked | Repo has adjacent pieces such as helper math and a typed `opponent_hand_type` head, but not the larger emission-model -> regime-state -> search/runtime-consumer chain this family requires. | docs explicit | Reconciliation explicitly keeps richer latent opponent posterior coupling on the reserve shelf rather than the active path. | not in code | No end-to-end coupled regime filter exists in code now: there is no evidenced emission model, no particle-side regime state carried through runtime, and no downstream consumer chain that would make the larger family real. | independently reproduced | Reconstructed from archive rejection logic and then checked against current repo surfaces to confirm that the missing pieces are not just labels but the entire emission-state-consumer chain the family depends on. | Pass 1 confirmed this is a larger-family rejection, not just a missing label-builder complaint. | Pass 2 + 3: keep the row separate from the smaller `opponent_hand_type` typed-hole claim. The strictest visible wording is that this larger family is blocked because the whole regime-coupled chain is missing, not just one target-builder surface. | trust medium | not ready | promise none | upside high | risk not rescored | not fallback-worthy | — | Reject/block row. It remains distinct from the narrower `opponent_hand_type` row so the reviewer can see that the archive kills the larger coupled-filter family for broader structural reasons than missing labels alone. |
| Under the current 2000 GPU-hour plan, Hydra’s budget fits conservative staged Hydra, not maximal Hydra with many sparse search-derived heads activated at once. | compute | `answer_3_combined.md:261-265`<br>`answer_13_combined.md:1351-1374` | “The compute story is internally contradictory.”<br>“The current budget fits **conservative staged Hydra**. It does **not** fit ‘turn on every advanced head...’” | partially supported | Repo/docs support this row only partially because the evidence is planning doctrine, staged sequencing, throughput assumptions, and archive arithmetic rather than a code-derived capacity theorem. The visible support is strong enough to justify a conservative staged reading of the current budget, but not strong enough to prove an exact hard ceiling for every maximal-Hydra variant. | docs explicit | Current docs explicitly discuss limited compute, staged sequencing, and the need to preserve throughput. The archive sharpens those ingredients into a conservative-versus-maximal framing, so docs support the direction of the claim even though the exact comparison sentence is archive synthesis rather than SSOT wording. | not in code | This is not an implementation-state claim. Nothing in code can be “live” for this row beyond the existence of partially closed expensive lanes; the row is a planning and feasibility judgment derived from docs plus archive arithmetic, so `no` here means “not a code behavior,” not “unsupported.” | independently reproduced | Reconstructed from archive budget arithmetic and then checked against current Hydra planning docs so the row depends on visible budget/sequencing doctrine rather than archive rhetoric alone. The independent reconstruction path is docs-plus-arithmetic, not runtime measurement. | Pass 1: the archive contributes a real compute-risk conclusion that was not already captured by narrower target-family rows. | Pass 2 + 3: keep the statement scoped to the current staged 2000 GPU-hour plan. The strictest visible wording is “budget supports conservative staged activation better than maximal simultaneous activation,” not “universal theorem about Hydra’s ultimate ceiling.” | trust medium | ready later | promise medium | upside medium | risk not rescored | fallback worthy | fallback if active path underdelivers early | This row is intentionally a planning judgment, not a code claim. The visible point is that current budget/doctrine supports staged closure and selective activation much more clearly than a maximal all-at-once rollout of sparse expensive loops. |
| Sparse advanced heads need density and transfer gates before activation; the archive’s stricter risk reading is that supervision density/interference is the real risk, not raw head-count alone. | training | `answer_3-1_combined.md:233-237`<br>`answer_13_combined.md:1220-1234`<br>`answer_13_combined.md:1252-1279`<br>`answer_13_combined.md:1397-1473` | “the target taxonomy is still underspecified”<br>“The real risk is lighting up sparse noisy heads before density and transfer exist.”<br>“Activate a head only if ... `rho_h >= 0.8` ... `spp_h >= 5`” | partially supported | Repo supports the premise more than the exact gate pack. Current code visibly has many advanced heads and slots, while several remain unpopulated, default-off, sparse, or semantically incomplete; that supports the archive’s concern about sparse/noisy activation. The explicit `rho_h`, `spp_h`, and gradient-conflict gates in this row are archive proposals responding to that visible risk, not current repo-implemented controls. | docs indirect | Current docs support staged activation, conservative target closure, and the idea that advanced lanes should not be turned on before they are semantically and operationally ready. They do not canonize the archive’s exact density/interference formulas, so docs support the discipline more than the metric details. | not in code | No density or transfer gate formulas are implemented in current code. What is live is the underlying risk shape the row responds to: many typed advanced surfaces exist, but several remain underfed, absent, default-off, or semantically incomplete in the current training path. | independently reproduced | Reconstructed from the archive’s long-run training-risk audit and then cross-checked against current repo evidence that the advanced-head problem is mainly one of sparse or weak supervision closure rather than literal lack of output surfaces. | Pass 1: this row survives because it expresses a concrete activation-discipline lesson that is broader than any one target-family defect row and more precise than a generic “too many heads” complaint. | Pass 2 + 3: keep the visible distinction explicit: the repo supports the risk premise, while the exact coverage/samples-per-parameter/conflict gates remain archive-proposed controls. The strictest wording is “density/interference is the real risk; exact gate formulas are still proposal-only.” | trust medium | ready later | promise medium | upside medium | risk not rescored | fallback worthy | fallback if sparse search heads keep starving | This row is about activation discipline, not model size alone. A reviewer should be able to see from this row why the archive demotes raw head-count fear and elevates sparse-supervision and transfer-interference fear instead. |
| The strongest surviving architecture claim from the archive is that Hydra should treat the fixed 192×34 tensor as a static compatibility path, then add a dedicated public-history path and stop routing tile-indexed policy through pooled-only features; the archive’s exact GRU-actor / transformer-learner package remains more forceful than fully settled. | architecture | `answer_21.md:2833-2854`<br>`answer_21.md:2919-2923`<br>`answer_21.md:2975-2995`<br>`answer_21.md:3054-3067`<br>`answer_21.md:3097-3102`<br>`answer_21.md:3240-3255`<br>`answer_21.md:3442-3457` | “Do **not** make Hydra a pure SE-ResNet project, and do **not** replace it with a pure transformer project.”<br>“The best architecture family for Hydra is an **asymmetric multi-path system**”<br>“Fix tile/global head routing first”<br>“Second: **dedicated history sidecar**, learner-first, actor-next.”<br>“Hydra should stop asking one pooled conv trunk to do three different jobs.” | partially supported | Current repo strongly supports two narrower parts of this row: the encoder already provides a fixed 192×34 static compatibility surface, and current model code visibly routes policy through pooled features even though spatial features are exposed. Repo support is only partial because the dedicated public-history path and the exact asymmetric GRU-actor / transformer-learner package are not implemented today; the row survives as a narrowed architecture judgment with one already-visible head-routing defect plus one archive-proposed history-path family expansion. | docs indirect | Current docs strongly support the ingredients behind the row—SE-style static trunk doctrine, selective search/distillation, richer opponent-history interest, and the caution that current tensor/state assumptions are contestable—but they do not explicitly canonize this exact family-level sentence or the exact actor/learner split. Docs support the problem framing more directly than the full stitched package. | partly in code | The static compatibility path, SE-style trunk family, and the pooled-versus-spatial head-routing seam are live in code now. The dedicated history encoder, late-fused history modulation, and explicit asymmetric actor/learner history modules are not in code. So the row is `partial`: one major corrective seam is already real, while the broader architecture family remains a proposal built around that seam. | independently reproduced | Reproduced by reading `answer_21.md`, then cross-checking the current encoder/model surfaces to verify three things locally: the 192×34 path is already the repo’s fixed compatibility surface, the backbone exposes both spatial and pooled outputs, and current policy inference still flows through pooled features. The stronger family claim about a dedicated history path was kept only after narrowing it to the archive’s most defensible core rather than the full exact module pack. | `answer_21.md` does contribute one real surviving architecture claim, but the family-level survivor is narrower than the answer’s most specific implementation bundle. The strongest support is for “static path + dedicated history path + spatial tile routing,” not for every exact hidden size / layer-count choice. | Pass 2 + 3: keep this row only in narrowed form. Preserve the family-level claim that fixed-tensor conv is only the static half of the answer and that pooled-only tile policy is too lossy, while explicitly refusing to over-canonize the exact GRU-actor / transformer-learner package as fully settled doctrine. | trust medium | ready later | promise high | upside high | risk not rescored | fallback worthy | fallback if history-sidecar evidence stays mixed but the spatial/global head split still wins | This row is intentionally narrower than the raw bottom-line package in `answer_21.md`. The canonical survivor is the architecture-family claim plus the head-routing correction, with the exact actor/learner module split kept proposal-level rather than treated as fully closed doctrine. |
## Appendix — archive file -> outcome
### Skipped files
- `reference_prompt_example_001_narrow_focused.md` -> skipped from extraction corpus
- `reference_prompt_example_002_broad_novel_fuse_loop.md` -> skipped from extraction corpus
- `reference_prompt_example_003_balanced_narrow_not_overconstrained.md` -> skipped from extraction corpus
### Processed files
- `answer_1_combined.md` -> merged away into another canonical row; broad docs-first handoff, no surviving canonical row after later stricter files
- `answer_1-1_combined.md` -> contributed surviving claim(s); early world-aware Hand-EV / unified-belief precursor used as predecessor support
- `answer_2_combined.md` -> contributed surviving claim(s); early public-teacher / privileged-vs-public target taxonomy and safety residual precursor support
- `answer_2-1_combined.md` -> contributed surviving claim(s); early narrow advanced-target-closure tranche and `exit_target`/`delta_q`/`safety_residual` boundary support
- `answer_3_combined.md` -> contributed surviving claim(s); compute-budget and program-coherence precursor support
- `answer_3-1_combined.md` -> contributed surviving claim(s); projected-belief object, reserve-shelf sequencing, and exactification staging precursor support
- `answer_5-1_combined.md` -> contributed surviving claim(s); budget-router/queue-scorer predecessor support for the tiny ponder-scorer lane
- `answer_6-1_combined.md` -> contributed surviving claim(s); runtime-first compression seam predecessor support
- `answer_7_combined.md` -> contributed surviving claim(s); safe search-distillation / `delta_q` target-closure predecessor support
- `answer_7-1_combined.md` -> contributed surviving claim(s); conservative ExIt / lower-confidence search-teacher predecessor support
- `answer_7-2_combined.md` -> merged away into another canonical row; lower-confidence teacher-distillation material folded into narrower ExIt/teacher rows rather than kept standalone
- `answer_8_combined.md` -> no surviving canonical claim; prompt-format / output-style analysis, not Hydra technical canon
- `answer_8-1_combined.md` -> contributed reject/block claim(s); precursor for the later-rejected posterior-consensus ExIt family
- `answer_8-2_combined.md` -> contributed surviving claim(s); action-sufficient CT-SMC world-compression predecessor support
- `answer_9_combined.md` -> contributed surviving claim(s); earlier broad target-activation draft kept mainly as contradiction evidence against later narrower target activation
- `answer_10_combined.md` -> contributed surviving claim(s); tiny ponder-scorer survivor and broad-router block
- `answer_11_combined.md` -> contributed surviving claim(s); narrow runtime-only compression survivor and benchmark-first framing
- `answer_12_combined.md` -> contributed reject/block claim(s); canonical reject source for posterior-consensus ExIt and regime-coupled opponent filtering
- `answer_13_combined.md` -> contributed surviving claim(s); long-run training-risk, density-gate, and conservative-budget claims added in this pass
- `answer_14_combined.md` -> contributed surviving claim(s); Hand-EV semantic repair, world-aware Hand-EV staging, and specialist endgame exactification
- `answer_15_combined.md` -> contributed surviving claim(s); target-provenance taxonomy, keep-off calls, and oracle interpretation
- `answer_16_combined.md` -> merged away into another canonical row; earlier rollout-threshold draft retained only as superseded contradiction evidence behind stricter `answer_16-1`
- `answer_16-1_combined.md` -> contributed surviving claim(s); stricter rollout trust boundary, retained `top2_policy_gap < 0.10`, and rejected-threshold cleanup
- `answer_17_combined.md` -> contributed surviving claim(s); Hand-EV naming/semantic mismatch and H1a repair detail
- `answer_18_combined.md` -> contributed surviving claim(s); strongest target-semantic audit and later narrowing of target activation claims
- `answer_19_combined.md` -> contributed surviving claim(s); exact narrow seam for the tiny learned scorer for the next ponder quantum
- `answer_20_combined.md` -> contributed surviving claim(s); cache/provenance authority critique and learner-only safe-policy recommendation
- `answer_21.md` -> contributed surviving claim(s); architecture-family narrowing kept only at the level of static compatibility path + dedicated history path + pooled-head-routing correction, with the exact asymmetric module package left proposal-level
