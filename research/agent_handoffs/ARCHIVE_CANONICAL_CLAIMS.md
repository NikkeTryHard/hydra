## Archive canonical claims ledger

This file is a stricter canonical ledger for the archive corpus in `research/agent_handoffs/combined_all_variants` only. Archive files are the extraction corpus. Current Hydra docs/code are used only for post-extraction validation and contradiction checks.

## Coverage

- total archive files processed: 27 non-reference `answer_*` combined archive files
- total archive files skipped: 3 `reference_prompt_example_*` files, skipped because they are prompt exemplars rather than prompt+answer claim records
- total raw claims extracted internally: 1069 candidate claim lines/blocks from the 27 processed archive files
- total canonical claims kept: 34
- total claims merged away: 1035 raw candidates collapsed during dedupe into duplicate rows or supporting/predecessor material for retained rows
- total claims rejected: 3 canonical claims retained only as reject/block or rejected-precision rows
- total unresolved / uncertain canonical claims: 14 rows still kept at `partial`, `blocked`, `medium`, `later`, or `unclear` after the stricter pass
- processed-file outcome accounting: 21 files contributed surviving claim(s); 2 files contributed reject/block claim(s); 3 files were merged away into other canonical rows; 1 processed file produced no surviving canonical claim
- coverage note: accounting here is file-exhaustive for the 27 processed `answer_*` files, but extraction was block-based rather than sentence-exhaustive. Early files were re-checked in this pass and are either cited into surviving rows or classified explicitly in the appendix.

## Legend

- `all_source_refs`: exact archive file and line spans supporting the canonical claim
- `repo_supported`: whether current Hydra docs/code support the claim judgment after extraction; status is explained in `repo_support_detail`
- `hydra_docs_present`: whether current Hydra docs explicitly carry the doctrine/claim; status is explained in `hydra_docs_detail`
- `in_code_now`: whether current code implements the claim substance today; status is explained in `in_code_detail`
- `reproduced`: whether the claim was independently reconstructed from archive lines plus current repo evidence; status is explained in `reproduced_detail`
- each row was re-checked through three internal validation passes; `validated_pass_2` records the strictest surviving visible judgment after pass-2 and pass-3 tightening so the table schema stays unchanged

| canonical_claim | tag | all_source_refs | supporting_source_quotes | repo_supported | repo_support_detail | hydra_docs_present | hydra_docs_detail | in_code_now | in_code_detail | reproduced | reproduced_detail | validated_pass_1 | validated_pass_2 | trustworthy | implementation_ready | promising | strength_upside | risk | fallback_worthy | fallback_role | notes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| The current `safety_residual` target object is semantically wrong in the archive-criticized/currently implemented form: it is built from a clipped `public_score - exact_risk` quantity rather than a true signed residual. | targets | `answer_18_combined.md:1294-1469`<br>`answer_18_combined.md:1848-1857`<br>`answer_2_combined.md:289-295` | “That is not a residual. It is either the unchanged public score or zero.”<br>“Use `Δ_s(a)` as the supervised target.”<br>“do **not** target exact danger directly” | yes | Current repo support is direct and local, not inferential: the replay loader already computes this target family, and the implemented value algebra matches the archive’s criticized formula rather than the archive’s proposed signed correction. This is a repo-supported defect claim because the bad formula is visible in the current target builder itself. | no | Current Hydra docs discuss advanced-target closure and caution around weak target semantics, but they do not explicitly document this exact algebraic defect in the current `safety_residual` target object or name the clipped public-minus-exact-risk quantity as a problem. | partial | The path is live end-to-end in training in the narrow sense that the target object, mask, batch carrier, head, and masked loss all exist today; it is only `partial` because the semantics of the built target are the criticized old version rather than the archive’s corrected version. | yes | Independently reconstructed from the archive critique and then re-derived from current code by checking that the replay loader builds the same clipped public-minus-exact-risk quantity the archive says is wrong, and that the batch/loss path really does consume it. | Pass 1: archive wording and current code agree that the shipped target algebra is not a true residual and therefore misnames the object it trains. | Pass 2 + 3: keep the row strictly descriptive and code-grounded; the visible evidence is strong enough to say the current target object is wrong, but not to mix that judgment with the proposed replacement formula. | high | no | low | medium | no | — | This row is intentionally limited to the currently implemented defect. The replacement formula is a separate canonical claim so the reviewer does not have to disentangle “what code does now” from “what the archive recommends instead.” |
| The archive’s retained repair for `safety_residual` is a signed residual correction (`exact_safety - public_score`), not another clipped conservative-bound slack. | targets | `answer_18_combined.md:1320-1399`<br>`answer_18_combined.md:1848-1857` | “Use `Δ_s(a)` as the supervised target.”<br>“the model learns how much the public score should move” | partial | Repo support is only partial because current code provides the exact place where this repair would land—the existing replay builder, target slot, mask path, and loss path—but the repaired formula itself is not implemented. The repo supports the seam, not the repaired semantics. | no | Current docs do not explicitly adopt this signed-residual formula. At most they support the broader idea that advanced targets need semantically credible construction before activation. | no | The repaired target is not in code now at all; the current builder still emits the older clipped target, so this row describes a proposal with a live insertion seam rather than any currently shipped behavior. | yes | Reconstructed from the archive’s explicit replacement formula and then checked against current code to verify that the target-builder seam, batching path, and loss surface already exist for this exact family. | Pass 1: the archive gives a concrete replacement formula, not just a complaint, and the formula is specific enough to be audited as its own canonical row. | Pass 2 + 3: keep this row proposal-only; the evidence supports “specific proposed repair with a live seam,” but not “repo-supported truth” or “docs-endorsed doctrine.” | medium | later | high | medium | yes | fallback if search-derived safety labels stay unsafe | Split from the defect row so a reviewer can separately judge whether the current code is wrong and whether the archive’s replacement is actually worth adopting. |
| After stricter archive follow-ups, `safety_residual` is the only advanced target family still left near-term activatable; earlier broader “activate several advanced targets now” language does not survive. | targets | `answer_2-1_combined.md:166-180`<br>`answer_9_combined.md:1095-1101`<br>`answer_15_combined.md:462-512`<br>`answer_18_combined.md:1288-1290` | “The three active targets in this tranche”<br>“Ship exactly three live advanced targets”<br>“Activate only `safety_residual_target`, and activate it narrowly” | partial | Repo support is comparative rather than absolute. The current repo makes this later-archive narrowing plausible because `safety_residual` already has a replay builder, mask, batch slot, head, and loss path, while ExIt still lacks a produced carrier, `delta_q` still lacks a semantically aligned normal training path, and belief/mix/hand-type remain semantically or infrastructurally open. | partial | Current docs support supervision-first closure and explicitly describe several advanced targets as missing production paths or requiring better semantics, but they do not themselves collapse that comparison into the exact sentence “only `safety_residual` remains near-term activatable.” That exclusivity remains archive-derived. | partial | Code is only `partial` support for this row because it shows one relatively closed advanced-target family (`safety_residual`) alongside several clearly unclosed ones; code does not itself encode a policy statement that only one family should be activated next. | yes | Reconstructed by re-reading the early broad tranche proposals and the later narrowing pass, then checking current repo closure target-by-target to see whether the later archive narrowing has a visible implementation basis. | Pass 1: the archive really does contradict itself here, with earlier files proposing a broader near-term tranche and later files narrowing it sharply. | Pass 2 + 3: keep only the later comparative judgment and state clearly that this is a closure comparison, not proof that `safety_residual` is already cleanly activatable without the semantic repair row above. | medium | later | high | medium | yes | fallback if search-label closure stays weak | The row is intentionally comparative. It says later archive passes leave `safety_residual` closest to near-term activation; it does not claim that the current `safety_residual` object is already semantically good enough to turn on unchanged. |
| The current Stage-A `belief_fields` teacher is a public projection with equalized hidden-zone totals and a uniform kernel, not a credible public posterior teacher. | belief | `answer_2_combined.md:217-247`<br>`answer_3-1_combined.md:233-237`<br>`answer_15_combined.md:213-316`<br>`answer_18_combined.md:1473-1568` | “these heads should learn the information state, not the hidden state”<br>“The right supervised object is the projected belief itself”<br>“This target is not a posterior over hidden state.” | yes | Repo support is direct because the current Stage-A teacher code actually constructs the criticized object: it projects belief mass into visible tables, equalizes hidden-zone totals, applies a uniform kernel, and then emits that product as the train-time teacher. The row is therefore supported by what the code literally builds, not by a speculative interpretation. | yes | Current docs explicitly back the same doctrinal distinction: belief supervision should target projected/public belief objects rather than raw hidden-state allocation or arbitrary heuristic field values. The docs therefore support the diagnosis that the current Stage-A teacher is not the final intended posterior object. | partial | The path is `partial` in code because the target family and batching route are live, but what they currently carry is the criticized Stage-A projection teacher rather than a semantically stronger public posterior teacher. | yes | Reproduced by reconstructing the archive’s criticism from current `teacher/belief.rs` behavior and then cross-checking that the target really is wired through the existing batch/loss path. | Pass 1: current code and archive language line up on the central point that Stage-A emits a projected belief artifact, not a validated posterior teacher object. | Pass 2 + 3: keep the descriptive diagnosis separate from the activation recommendation, and state the concrete reason for `partial`: the path exists, but the teacher semantics are the weaker Stage-A version. | high | no | low | low | no | — | This row is about what the current teacher object is. The policy judgment “therefore keep it off” is intentionally in the next row so the reviewer does not have to infer causal structure from compressed wording. |
| Because the current Stage-A `belief_fields` teacher is only a public projection, the archive keeps `belief_fields` supervision off rather than treating it as a credible active-path target. | belief | `answer_15_combined.md:281-316`<br>`answer_18_combined.md:1559-1568` | “keep `w_belief_fields = 0.0`”<br>“`belief_fields`: semantically wrong, not privileged, off” | yes | Repo supports the recommendation in the limited but concrete sense that the target family already exists and is wired, while the current teacher semantics remain the weaker projection-only version described in the previous row. The recommendation is therefore grounded in a visible mismatch between available plumbing and inadequate teacher semantics. | yes | Current docs support this keep-off posture because they explicitly prefer semantically credible public-teacher objects and conservative staged activation over turning on weakly defined belief supervision early. | partial | The code state is `partial`: the target family is present and can be carried through training machinery, but it stays default-off and semantically untrusted because the teacher object has not met the stronger posterior-teacher standard. | yes | Reproduced by combining the archive’s explicit keep-off language with the current code fact that the target path exists but is still carrying the criticized Stage-A teacher object. | Pass 1: the archive clearly recommends keeping this target off until the teacher object is more credible than the current Stage-A projection. | Pass 2 + 3: keep the recommendation row explicit and self-sufficient; the visible reason for “off” is not mere absence, but the mismatch between live plumbing and weak current teacher semantics. | high | no | low | low | no | — | This row exists so the reviewer does not have to infer the policy consequence from the descriptive semantic row above. The answer is visible here: the path exists, but the archive still says to keep it off because the teacher object is not yet good enough. |
| The current Stage-A `mixture_weight` path inherits the same projection-teacher problem as `belief_fields` and also lacks a canonical component-identity contract. | belief | `answer_2_combined.md:217-247`<br>`answer_15_combined.md:295-316`<br>`answer_18_combined.md:1572-1600` | “Because component order is arbitrary”<br>“it inherits the belief failure and adds label-switching risk”<br>“`mixture_weight`: semantically wrong” | partial | Repo directly supports only two hard facts: Stage-A `mixture_weights` come from the same projection teacher as `belief_fields`, and no canonical component-ID contract is documented in code. The stronger label-switching diagnosis is an inference from that missing canonicalization. | partial | Current docs support projected/public belief doctrine but do not define stable mixture component identities. | partial | Typed surface exists, Stage-A can emit weights, and code sorts mixture components at runtime, but no cross-sample canonical component contract closes the semantics. | yes | Reconstructed from archive lines plus Stage-A teacher code and bridge-side component sorting. | Pass 1 showed a real semantic and identity problem. | Pass 2 narrows the wording to “lacks a canonical identity contract” instead of overstating instability as a proven theorem. | medium | no | low | medium | no | — | Descriptive row only. The keep-off policy is split out below. |
| Because the current `mixture_weight` teacher is projection-only and component identity is not canonically closed, the archive keeps `mixture_weight` supervision off. | belief | `answer_15_combined.md:295-316`<br>`answer_18_combined.md:1594-1600` | “keep `w_mixture_weight = 0.0`”<br>“`mixture_weight`: semantically wrong, not privileged, off” | partial | Repo supports the archive’s keep-off conclusion, but the strongest component-identity critique remains inference from missing canonicalization rather than explicit repo doctrine. | partial | Docs support caution and projected/public teacher semantics, but do not explicitly discuss mixture-weight activation policy in this exact language. | partial | Target path exists and is default-off; no code path closes the semantics well enough to justify activation. | yes | Reconstructed from archive keep-off language plus current teacher path. | Pass 1 supported the keep-off rule. | Pass 2 keeps it separate from the descriptive teacher/identity defect row. | medium | no | low | medium | no | — | Recommendation row only. |
| The current dense `[B,46]` `delta_q` training path is not closed: the model head is `[46]`, the runtime/search feature seam is discard-level `[34]`, and the normal training path still has no populated builder or normal-batch target. | targets | `answer_2-1_combined.md:223-227`<br>`answer_7_combined.md:295-298`<br>`answer_7-1_combined.md:339-357`<br>`answer_15_combined.md:318-423`<br>`answer_16-1_combined.md:344-346`<br>`answer_18_combined.md:1604-1645` | “with an action mask keeping only actions with meaningful search support”<br>“`sample.rs` still leaves `delta_q_target: None`”<br>“runtime `delta_q` is `[34]`, not `[46]`” | yes | Repo support is direct and multi-part: the model exposes a dense `[46]` `delta_q` head, `HydraTargets` exposes a dense slot, normal sample conversion still leaves that target absent, and bridge/runtime code exports only tile/discard-level `[34]` values. The row is therefore supported not by one missing piece but by a visible mismatch across head shape, runtime feature semantics, and sample-generation state. | partial | Current docs explicitly say `delta_q_target` lacks a production path and that search-derived labels must be provenance-explicit. They only partially support the full row because the concrete `[46]`-versus-`[34]` mismatch is mainly a code-level finding rather than a docs-level explanation. | partial | `partial` here means two different surfaces exist today—a model-side training surface and a runtime/search feature surface—but the repo does not yet connect them through one semantically aligned, masked, end-to-end normal training path. | yes | Reconstructed by checking archive language against current `model.rs`, `sample.rs`, `losses.rs`, and `bridge.rs`, then confirming that the runtime and training surfaces called “delta_q” are not the same object in shape or provenance. | Pass 1: the row is supported because the repo really does expose both a dense training head and a narrower runtime/search feature seam under the same family name. | Pass 2 + 3: keep the wording concrete and self-sufficient—`delta_q` is neither wholly absent nor credibly closed; it is a mismatched set of surfaces that do not yet form one valid normal training path. | high | blocked | medium | medium | yes | fallback if future search labels are chosen-action-only | This row intentionally says what “not closed” means: a head exists, a runtime feature exists, but the builder/provenance/masking story that would make them one trainable target family is still missing. |
| `opponent_hand_type` is a typed hole: the head exists, but the normal training path still has no ontology, no mapping spec, and no populated target builder. | targets | `answer_2_combined.md:258-277`<br>`answer_15_combined.md:320-359`<br>`answer_18_combined.md:1649-1675`<br>`answer_13_combined.md:1428-1431` | “Use the archetype set already implied”<br>“no taxonomy, no mapping”<br>“Stop spending attention on it until labels exist.” | yes | Repo evidence is direct: the output surface exists, `HydraTargets` has the slot, normal sample conversion always leaves it `None`, and no ontology/mapping/builder is present in current code. | partial | Docs keep richer opponent modeling on the reserve shelf, but they do not define the ontology or mapping needed to make this target active-path ready. | partial | Only the typed output surface exists today; no ontology-backed target object or populated normal training path is live. | yes | Reconstructed from archive lines plus current `model.rs` and `sample.rs` target conversion. | Pass 1 confirmed this is a typed hole, not an active target path. | Pass 2 keeps the descriptive typed-hole claim separate from the keep-off recommendation below. | high | blocked | low | low | no | — | Descriptive row only. |
| Because `opponent_hand_type` lacks an ontology-backed builder and populated target path, the archive keeps it off rather than activating it on weak labels. | targets | `answer_15_combined.md:350-359`<br>`answer_18_combined.md:1669-1675`<br>`answer_13_combined.md:1428-1431` | “Do not populate it until a class ontology and mapping spec exist.”<br>“`opponent_hand_type`: typed hole … off” | yes | Repo supports the keep-off conclusion because the head surface exists without the ontology/builder chain needed to make the target meaningful. | partial | Docs imply reserve-shelf timing and later richer opponent modeling, but do not provide activation criteria beyond the missing ontology. | partial | Still off in practice because there is no populated target path to train it. | yes | Reconstructed from archive keep-off language plus the current missing ontology/builder chain. | Pass 1 supported the keep-off recommendation. | Pass 2 keeps the recommendation separate from the descriptive typed-hole row. | high | no | low | low | no | — | Recommendation row only. |
| `oracle_critic` is currently a detached optional privileged branch: the target path exists, the head is live, and the oracle branch is fed from a detached pooled representation. | targets | `answer_2_combined.md:188-216`<br>`answer_15_combined.md:498-512`<br>`answer_18_combined.md:1679-1704`<br>`answer_13_combined.md:1411-1414` | “Used only for `oracle_critic`”<br>“Keep `oracle_target` / oracle critic on the privileged side only”<br>“the oracle head currently cannot shape the shared representation” | partial | Repo code proves three narrower facts: oracle target plumbing exists, the head is live, and the oracle path is detached and default-optional. Repo does not add a stronger enforcement boundary beyond that. | partial | Current docs explicitly warn against expanding privileged pathways before public-target closure and note the detached oracle path. | partial | Fully implemented as an optional detached auxiliary branch, but not an active-path closure mechanism. | yes | Reconstructed from archive lines plus `model.rs` and current loss defaults. | Pass 1 confirmed the detached optional branch description. | Pass 2 keeps this as a descriptive role/state row only. | medium | no | low | low | no | — | Descriptive row only. |
| The archive explicitly treats `oracle_critic` as non-closure for the student path: it should not be counted as evidence that advanced public/student targets are already solved. | targets | `answer_15_combined.md:503-512`<br>`answer_18_combined.md:1697-1704` | “do not count it as tranche acceptance”<br>“privileged-only diagnostic” | partial | Repo supports the warning indirectly: the branch is detached and optional, so it cannot by itself prove that student-facing target closure exists. | partial | Current docs support the same sequencing rule: privileged guidance later, public-target closure first. | no | No code path turns oracle presence into student-target closure; that would be an interpretation error, not an implementation state. | yes | Reconstructed from archive sequencing language plus the current detached optional branch. | Pass 1 supported the warning. | Pass 2 keeps it as a separate interpretive/recommendation row. | medium | no | low | low | no | — | Recommendation/interpretation row only. |
| Conservative ExIt remains doctrinally alive in the archive, but only in a later masked policy-head reuse form rather than as broad immediate search imitation. | search | `answer_2-1_combined.md:209-221`<br>`answer_7-1_combined.md:312-357`<br>`answer_9_combined.md:1232-1238`<br>`answer_9_combined.md:1280-1325`<br>`answer_15_combined.md:479-483` | “ExIt target”<br>“planner improves policy; apprentice imitates planner”<br>“masked, partial-policy distillation” | partial | Repo supports the narrowed doctrinal claim: ExIt remains central in docs and loss helper logic, but current active-path closure is still later and masked rather than broad immediate use. | yes | Current docs keep ExIt doctrinally central while also saying its production path is not yet closed. | no | The doctrinal claim itself is not a live code path; it is a retained archive/docs direction. | yes | Reconstructed from archive doctrine rows plus current ExIt docs. | Pass 1 confirmed ExIt remains alive as doctrine. | Pass 2 keeps doctrine separate from the missing-carrier repo-state row below. | medium | later | medium | medium | yes | fallback if explicit search-policy batches are added later | Doctrine row only. The missing carrier/path is split out below. |
| Current Hydra still lacks an evidenced end-to-end `exit_target` carrier: helper/loss-side support exists, but the normal sample and batch path do not yet produce a real upstream `exit_target`. | search | `answer_2-1_combined.md:205-207`<br>`answer_2-1_combined.md:279-286`<br>`answer_15_combined.md:479-483` | “add exactly one new training capability: optional advanced-target plumbing”<br>“Produced: `exit_target`”<br>“`exit_target` is not actually carried anywhere” | partial | Repo evidence is concrete: loss-side helper support exists and docs expect the path, but current normal sample/batch production still does not populate a real upstream `exit_target` carrier. | yes | Reconciliation explicitly says upstream `exit_target` production still needs to become real. | partial | Loss-side consumption support exists; no active end-to-end normal-batch carrier exists today. | yes | Reconstructed from archive lines plus current helper/loss code and missing sample path. | Pass 1 confirmed carrier/path absence. | Pass 2 keeps this descriptive repo-state row separate from the ExIt doctrine row. | medium | blocked | medium | medium | yes | fallback if search-policy targets are added before broader search | Descriptive repo-state row only. |
| Current Hand-EV field names overclaim exactness: `tenpai_prob`, `win_prob`, and especially `expected_score` name stronger semantics than the live heuristic formulas actually provide. | hand_ev | `answer_17_combined.md:1163-1370`<br>`answer_14_combined.md:241-307` | “This is not semantically ‘probability of reaching tenpai…’”<br>“It is not a clean probability of winning.”<br>“The score field is unsalvageable as a live semantic claim.” | yes | Repo directly implements the criticized formulas: heuristic continuation boosts, fallback base-win logic, and `expected_score = win_prob[2] * conditional_score_estimate(...)`. | partial | Reconciliation says current Hand-EV win/score modeling remains heuristic; docs name the channels but do not validate exact probabilistic semantics. | yes | Fully live in current runtime feature computation, but with names that overclaim the formula semantics. | yes | Reconstructed from archive lines plus exact formula reads in `hand_ev.rs`. | Pass 1 confirmed the semantic mismatch. | Pass 2 keeps the wording narrower than “all Hand-EV is fake”; the problem is semantic overclaim in the named outputs. | high | no | medium | medium | no | — | Descriptive semantic row only. |
| H1a exact one-step Hand-EV semantic repair is the archive’s leading immediate Hand-EV proposal. | hand_ev | `answer_14_combined.md:181-199`<br>`answer_14_combined.md:308-367`<br>`answer_17_combined.md:1300-1345` | “This is the strongest immediate Hand-EV upgrade”<br>“Try first: H1a exact one-step Hand-EV semantic repair” | partial | Repo supports the proposal’s insertion seam and sequencing fit, but not the proposal itself as current code. | partial | Current docs support Hand-EV realism before deeper AFBS, but do not canonize the exact H1a formula set. | no | Not in code now; this is a retained archive proposal, not a live behavior. | yes | Reconstructed from archive proposal rows plus the existing Hand-EV seam. | Pass 1 confirmed H1a as the leading immediate archive proposal. | Pass 2 keeps it as proposal-only and avoids claiming repo endorsement. | medium | later | high | medium | yes | fallback if deeper search stays deferred | Proposal ranking row only. The concrete insertion seam is split below. |
| The retained H1a Hand-EV repair proposal reuses the current `HandEvFeatures` / 42-plane interface and existing bridge-to-encoder insertion seam rather than requiring new heads. | hand_ev | `answer_14_combined.md:194-199`<br>`answer_14_combined.md:308-367` | “do not change the tensor interface”<br>“keep the current 42-channel layout” | yes | Repo directly supports this seam claim because the current Hand-EV payload already flows through `HandEvFeatures`, bridge code, and encoder channels. | partial | Current docs support Hand-EV realism earlier than deeper search, but the seam detail is more explicit in the archive than in docs. | partial | The interface is live in code now; the repaired semantics are not. | yes | Reconstructed from archive seam language plus current `hand_ev.rs` and bridge wiring. | Pass 1 confirmed a real seam with no new-head requirement. | Pass 2 keeps the seam claim separate from the proposal-ranking claim above. | high | later | medium | low | yes | fallback if feature-surface churn is undesirable | Concrete insertion-seam row only. |
| A real runtime seam already exists for world-aware CT-SMC Hand-EV: bridge/runtime code can condition Group-D-style Hand-EV features on `SearchContext.ct_smc`. | hand_ev | `answer_1-1_combined.md:341-350`<br>`answer_14_combined.md:200-216`<br>`answer_14_combined.md:369-427` | “Do not compute Hand-EV from one marginal count vector if CT-SMC exists.”<br>“SearchContext.ct_smc exists” | yes | Repo directly supports the runtime seam: current bridge logic dispatches to CT-SMC-aware Hand-EV when CT-SMC context is present. | partial | Docs imply CT-SMC as search-grade posterior and later selective improvements, but do not foreground this seam as active doctrine. | partial | Runtime seam exists, but it is not yet a closed train/infer parity path. | yes | Reconstructed from archive seam rows plus current bridge/runtime behavior. | Pass 1 confirmed a real runtime seam. | Pass 2 keeps this descriptive seam row separate from the parity blocker and second-stage policy rows below. | high | later | medium | low | yes | fallback if public-count Hand-EV realism saturates | Descriptive seam row only. |
| Train/infer parity for world-aware CT-SMC Hand-EV is not closed: the current training path does not reconstruct matching search-context observations for standard model-input use. | hand_ev | `answer_14_combined.md:453-454`<br>`answer_14_combined.md:502-559` | “loader does not build search-context observations”<br>“[blocked] for immediate mainline” | yes | Repo evidence directly supports the blocker: runtime can condition on CT-SMC context, but the standard training pipeline does not build matching search-context observations. | partial | Docs imply the same blocker indirectly by keeping this lane later and specialist, but the parity problem is more explicit in the archive than in docs. | partial | Runtime-only seam exists; no closed train-side re-encode path exists for standard mainline training. | yes | Reconstructed from archive blocker lines plus current loader/runtime surfaces. | Pass 1 confirmed the parity blocker. | Pass 2 keeps the blocker separate from the second-stage recommendation below. | high | blocked | medium | medium | no | — | Descriptive blocker row only. |
| Because train/infer parity is not closed and representative-world compression must clear its own gates, the archive keeps world-aware CT-SMC Hand-EV as second-stage only. | hand_ev | `answer_14_combined.md:612-616` | “Try second, only if H1a wins and CT-SMC gates pass” | partial | Repo supports the prerequisites and blockers but does not implement the promotion itself. | partial | Current docs support later selective improvement rather than immediate promotion. | no | Not in code now as an active-path feature. | yes | Reconstructed from archive staging language plus current blocker status. | Pass 1 confirmed second-stage-only status. | Pass 2 keeps the policy/staging claim separate from seam and parity rows. | medium | later | high | medium | yes | fallback if public-count Hand-EV realism underdelivers | Staging row only. |
| Current endgame code is a selective particle-weighted shell, not a true exact multiplayer endgame solver. | runtime | `answer_14_combined.md:221-235`<br>`answer_14_combined.md:548-559` | “The shell already exists”<br>“full multiplayer exact endgame solving ... does not survive” | yes | Repo strongly supports this description: current endgame code gates on wall/threat and aggregates `eval_fn` over selected particles. | yes | Reconciliation places exactification/endgame on the reserve shelf and after supervision/feature realism work. | partial | A specialist particle shell is live in code; it is not a true exact multiplayer solver. | yes | Reconstructed from archive lines plus direct `endgame.rs` behavior. | Pass 1 confirmed the shell-vs-exactness distinction. | Pass 2 keeps this descriptive code-state claim separate from the later specialist recommendation below. | high | no | low | low | no | — | Descriptive code-state row only. |
| The archive retains stronger endgame exactification only as a later specialist leaf inside the existing particle shell, not as a broad mainline search identity. | runtime | `answer_3-1_combined.md:250-256`<br>`answer_14_combined.md:600-616` | “keep, phase-next #1”<br>“Endgame exactification is later and narrower” | partial | Repo supports the shell and insertion seam, but the stronger exact leaf is not implemented. | yes | Current docs explicitly keep selective exactification/endgame on the reserve shelf and later path. | no | Not implemented as a stronger exact leaf today. | yes | Reconstructed from archive staging/recommendation language plus current shell status. | Pass 1 confirmed later-specialist positioning. | Pass 2 keeps the policy/staging claim separate from the descriptive shell row above. | medium | later | medium | low | yes | fallback if late-game specialist value is proven offline | Recommendation/staging row only. |
| A tiny learned scorer for the next ponder quantum is a narrow surviving compute-allocation idea retained across the archive. | search | `answer_5-1_combined.md:402-406`<br>`answer_10_combined.md:205-217`<br>`answer_19_combined.md:1631-1660` | “replace the current heuristic hard-state trigger”<br>“Replace the scalar heuristic with a tiny cost-sensitive scorer”<br>“Learn exactly one thing” | partial | Repo supports a precise insertion seam: queue ordering is already scalar and local, but the learned scorer and its offline labels do not exist yet. | partial | Current docs support selective compute and pondering importance, but they do not promote a learned tiny scorer into active doctrine. | no | Current code still uses the hand-written ponder-priority heuristic. | yes | Reconstructed from archive proposal rows plus current queue surfaces. | Pass 1 confirmed a real narrow compute-allocation proposal. | Pass 2 keeps the idea row separate from the replacement-target seam below. | medium | later | medium | low | yes | fallback if broad routing stays unjustified | Proposal-identity row only. |
| The intended seam for that tiny scorer is replacement of the current queue-priority heuristic, not introduction of a full runtime router or learned halting policy. | search | `answer_10_combined.md:213-217`<br>`answer_19_combined.md:1635-1645`<br>`answer_19_combined.md:1909-1927` | “Replace the scalar heuristic”<br>“Do **not** learn ... a full runtime router”<br>“Use a sign-constrained linear model.” | yes | Repo directly supports the seam: `compute_ponder_priority` already exists as a scalar heuristic, and `PonderTask.priority_score` is the current insertion point. | partial | Docs describe the seam indirectly through pondering doctrine and hard-state compute, but the “replace exactly this heuristic” framing is archive-specific. | partial | The target heuristic seam is live; the learned replacement is not. | yes | Reconstructed from archive lines plus `afbs.rs` queue surfaces. | Pass 1 confirmed this is a seam claim, not a broader router claim. | Pass 2 keeps it separate from the “surviving idea” row above. | high | later | medium | low | yes | fallback if queue ranking still matters after cache/provenance fixes | Concrete seam row only. |
| A broad multi-arm learned router is blocked by current runtime reality: the cited live inference path is still basically cache hit or actor+SaF, not a rich menu of callable arms. | architecture | `answer_10_combined.md:197-201`<br>`answer_10_combined.md:233-245` | “full learned router ... is blocked by current runtime surfaces”<br>“Right now this would mostly be a paper design” | yes | Repo strongly supports this blocking judgment because current inference exposes only cache-hit reuse or actor+SaF fast path for the cited live path. | yes | Reconciliation explicitly rejects broad “search everywhere” rollout and keeps AFBS specialist/hard-state gated. | yes | Fully true in code as a negative statement: current runtime does not implement a broad multi-arm router. | yes | Reconstructed from archive lines plus direct read of current inference runtime. | Pass 1 confirmed the block. | Pass 2 keeps the wording narrow to the cited current runtime surface. | high | no | low | low | no | — | Negative prioritization row only. |
| Current cached ponder/rollout authority has weak semantic identity and provenance: cache entries are keyed too weakly and `PonderResult` lacks the provenance fields needed to justify live authoritative reuse. | rollout | `answer_13_combined.md:865-884`<br>`answer_16-1_combined.md:350-354`<br>`answer_20_combined.md:1030-1124`<br>`answer_20_combined.md:1126-1231` | “authority without semantic identity”<br>“no `source_net` / `source_version` on `PonderResult`”<br>“same observation hash ... does not [imply same semantic root]” | yes | Repo support is strong because the criticized ingredients are all present in the current runtime path: cache lookup is keyed by a plain state hash, `PonderResult` carries only shallow summary fields such as policy/value/depth/visits/timestamp, and inference can early-return on a cache hit without provenance-rich identity or trust-class checks. | partial | Current docs support the general doctrine that search-derived signals need provenance and that AFBS should remain specialist/gated, but they do not explicitly document the current cache-authority bug in the same concrete terms as the archive/code audit. | yes | `yes` here means the criticized behavior is happening now in live code: cache hits can directly influence action selection, while the identity fields that would justify that authority—source net, source version, belief/runtime cohort, trust class—are absent. | yes | Reconstructed from archive provenance criticism and then independently rechecked against the current cache/result/inference surfaces to confirm that the runtime really can reuse these weakly identified summaries as authoritative outputs. | Pass 1: the row survives because the provenance weakness is not a vague design concern; it is a visible property of the current cache key, result payload, and inference early-return path. | Pass 2 + 3: keep the row purely descriptive and explicit about what is missing, so the reviewer can see the exact basis for the “yes” and “in code now” judgments without needing the recommendation row below to interpret it. | high | now | medium | high | no | — | The key distinction is visible in this row: current runtime authority is real, and provenance weakness is real. The policy answer to that problem—“therefore keep these outputs learner-only”—is intentionally separated into the next row. |
| The archive’s safe-policy recommendation is to keep current ponder/rollout outputs learner-only and off the live decisive authority path until provenance and admission closure exist. | rollout | `answer_16-1_combined.md:367-379`<br>`answer_20_combined.md:1409-1549` | “Do **not** let RolloutNet participate in live decisive AFBS root computation”<br>“all current ponder/rollout outputs should remain learner-only” | no | Repo support is `no` because current code does not implement this learner-only safety policy; the live runtime still permits the cache-hit authority behavior criticized in the preceding row, so the row describes archive recommendation rather than current repo state. | partial | Current docs partially support the recommendation in a doctrinal sense—they favor conservative specialist AFBS, provenance-aware search labels, and a fast actor+SaF mainline—but they do not literally say that current cached ponder outputs are already kept off the decisive authority path. | no | This policy is not in code now. Current runtime still allows authoritative reuse of cache-hit summaries, so the learner-only posture described here remains a recommended future runtime policy rather than a shipped behavior. | yes | Reconstructed from explicit archive policy language and then checked against current runtime behavior to confirm that the recommendation is not already implemented and therefore must be described as recommendation-only. | Pass 1: the archive clearly advances a specific safe-policy recommendation rather than merely criticizing the current cache path. | Pass 2 + 3: keep the row explicit that this is a recommendation and not a repo fact; the strictest surviving wording is “recommended later policy in response to the provenance problem,” not “current Hydra runtime doctrine.” | medium | later | medium | high | yes | fallback if provenance never closes | This row answers the “what should policy be?” question raised by the prior provenance row. It stays separate so the table does not accidentally turn an archive recommendation into a claim about what current Hydra already does. |
| After stricter archive revalidation, the only exact rollout-admission threshold retained is `top2_policy_gap < 0.10`. | rollout | `answer_16-1_combined.md:336-345`<br>`answer_16-1_combined.md:389-415` | “Grounded hard-state minimum: `top2_policy_gap < 0.10`”<br>“These exact thresholds survive revalidation” | partial | Repo supports the gap signal and the 0.10-style hard-state minimum, but not the broader earlier exact rollout thresholds. | partial | Current docs explicitly describe small top-2 gap hard states and preserve this exact threshold more clearly than other rollout thresholds. | partial | Code uses the gap signal in runtime heuristics, but does not implement a fully explicit rollout-admission regime from this one threshold alone. | yes | Reconstructed from stricter archive revalidation plus current queue/runtime signal surfaces. | Pass 1 confirmed one exact threshold survives. | Pass 2 keeps this precise retained-threshold claim separate from the rejected-threshold cleanup row below. | medium | later | low | medium | yes | fallback if rollout trust rules stay empirical | Retained-threshold row only. |
| Earlier exact rollout numeric thresholds such as `risk_score > 0.08` and `particle_ess < 0.55` are explicitly rejected or demoted by the stricter archive revalidation. | rollout | `answer_16_combined.md:168-177`<br>`answer_16_combined.md:214-227`<br>`answer_16-1_combined.md:417-440` | “`risk_score > 0.08` ... `[inference]`”<br>“I am **not** keeping the earlier exact `risk_score` and `particle_ess` cutoffs.”<br>“Rejected: `risk_score > 0.08` and `particle_ess < 0.55`” | yes | Repo exposes the signals but does not support those earlier exact numeric cutoffs; the stricter archive follow-up explicitly removes them. | partial | Docs describe risk/ESS qualitatively, not with those rejected numbers. | no | Those rejected thresholds are not codified in code. | yes | Reconstructed from earlier archive draft vs stricter follow-up comparison plus current signal surfaces. | Pass 1 confirmed a real archive contradiction. | Pass 2 keeps this as a rejected-precision cleanup row rather than letting the earlier numbers leak into current doctrine. | high | no | no | low | no | — | Reject/cleanup row. `answer_16-1` is canonical over `answer_16`. |
| Action-sufficient CT-SMC world compression is retained only as a narrow benchmark-first project: a runtime-only, discard-phase, equal-budget falsification lane rather than a current active-path method. | fallback | `answer_6-1_combined.md:467-474`<br>`answer_8-2_combined.md:304-346`<br>`answer_11_combined.md:586-690` | “no v0 changes ... Runtime first; distillation / labels later”<br>“Action-sufficient CT-SMC world compression”<br>“worth it now only as a very narrow, runtime-only falsification project” | partial | Repo supports only the enabling seams: CT-SMC particles, Hand-EV geometry, and endgame particle shell. Repo does not implement the compression operator itself. | no | Current docs do not elevate this lane into active doctrine; it remains an archive research candidate. | no | Not in code now; only the adjacent seams exist. | yes | Reconstructed from archive benchmark-first compression rows plus current CT-SMC/endgame/Hand-EV seams. | Pass 1 confirmed a narrow surviving compression lane. | Pass 2 keeps it as benchmark-only and avoids restating killed broader descendants inside the same row. | medium | later | medium | medium | yes | fallback if runtime cost stays too high | Kept narrow on purpose; broader compression descendants were not revived in this pass. |
| Posterior-consensus ExIt over hidden worlds is rejected as currently scoped because the required public world-conditioned action-teacher object is missing and the idea collapses into ordinary uncertainty-weighted ExIt when narrowed. | search | `answer_8-1_combined.md:306-342`<br>`answer_12_combined.md:137-151`<br>`answer_12_combined.md:173-214` | “Posterior-consensus ExIt/ΔQ distillation”<br>“Verdict: kill as currently scoped”<br>“it reduces to ordinary ExIt with confidence weighting” | blocked | Repo does not provide the public world-conditioned action-teacher object the archive says is required; without it, the family’s distinctive mechanism is not constructible from current surfaces. | partial | Current docs keep narrower ExIt and target-closure work alive, but do not define the required world-conditioned public teacher object. | no | No such mechanism exists in current code. | yes | Reconstructed from archive rejection plus current absence of the required teacher object/path. | Pass 1 confirmed this family dies on missing teacher-object semantics, not just missing plumbing. | Pass 2 keeps it as a reject row, not a deferred survivor. | medium | no | no | high | no | — | Reject/block row. |
| Regime-coupled opponent filtering / latent opponent posterior feedback is rejected as currently scoped because the required emission model, particle-side regime state, and downstream consumers are missing from the evidenced surfaces. | architecture | `answer_12_combined.md:153-166`<br>`answer_12_combined.md:173-214` | “Verdict: kill.”<br>“No such grounded module or label builder is evidenced.” | blocked | Repo has adjacent pieces such as helper math and a typed `opponent_hand_type` head, but not the larger emission-model -> regime-state -> search/runtime-consumer chain this family requires. | yes | Reconciliation explicitly keeps richer latent opponent posterior coupling on the reserve shelf rather than the active path. | no | No end-to-end coupled regime filter exists in code now. | yes | Reconstructed from archive rejection plus current absence of the required chain. | Pass 1 confirmed this is a larger-family rejection, not just a missing label-builder complaint. | Pass 2 keeps it separate from the smaller `opponent_hand_type` typed-hole row. | medium | no | no | high | no | — | Reject/block row. |
| Under the current 2000 GPU-hour plan, Hydra’s budget fits conservative staged Hydra, not maximal Hydra with many sparse search-derived heads activated at once. | compute | `answer_3_combined.md:261-265`<br>`answer_13_combined.md:1351-1374` | “The compute story is internally contradictory.”<br>“The current budget fits **conservative staged Hydra**. It does **not** fit ‘turn on every advanced head...’” | partial | Repo/docs support this row only partially because the evidence is planning and throughput doctrine plus archive arithmetic, not a code-level proof. The visible support is strong enough to say the budget favors staged, conservative activation, but not strong enough to claim a formally measured hard ceiling for every maximal-Hydra variant. | yes | Current docs explicitly discuss limited compute, staged sequencing, and the need to preserve throughput. The archive sharpens that doctrine into a specific contrast: conservative staged Hydra looks budget-plausible; maximal Hydra with many sparse expensive loops does not. | no | This row is not an “in code now” statement at all. It is a planning and feasibility judgment derived from current docs, archive arithmetic, and current throughput assumptions, so `no` means “not a code behavior,” not “unsupported.” | yes | Reconstructed from archive budget arithmetic and then checked against current Hydra planning docs so the row does not depend on archive rhetoric alone. The reproduction path is docs-plus-arithmetic, not runtime inspection. | Pass 1: the archive contributes a real compute-risk judgment that was not already captured by the narrower target rows. | Pass 2 + 3: keep the wording conservative and visible—this is a plan-feasibility claim, not a theorem about every possible Hydra scaling path, so the row explicitly anchors itself in the current 2000 GPU-hour staged plan. | medium | later | medium | medium | yes | fallback if active path underdelivers early | The row is intentionally scoped to the current staged budget plan. It should be read as “budget supports conservative staging more than maximal simultaneous activation,” not as a universal claim about Hydra’s ceiling under all future resource assumptions. |
| Sparse advanced heads need density and transfer gates before activation; the archive’s stricter risk reading is that supervision density/interference is the real risk, not raw head-count alone. | training | `answer_3-1_combined.md:233-237`<br>`answer_13_combined.md:1220-1234`<br>`answer_13_combined.md:1252-1279`<br>`answer_13_combined.md:1397-1473` | “the target taxonomy is still underspecified”<br>“The real risk is lighting up sparse noisy heads before density and transfer exist.”<br>“Activate a head only if ... `rho_h >= 0.8` ... `spp_h >= 5`” | partial | Repo supports the premise but not the full gate pack. Current code visibly has many dormant or underfed advanced surfaces, which supports the archive’s concern about sparse/noisy activation; however, the explicit density, samples-per-parameter, and gradient-conflict gates named in the row are archive additions rather than current repo-implemented controls. | partial | Current docs support staged activation, conservative target closure, and “do not light up weakly justified advanced loops too early,” but the specific gate formulas—coverage thresholds, samples-per-parameter minimums, and conflict checks—are more explicit in the archive than in current docs. | no | Those concrete gate formulas are not in code now. What is in code now is the underlying risk condition they respond to: many advanced heads exist, but several remain absent, default-off, sparse, or semantically incomplete in the current training path. | yes | Reconstructed from the archive’s long-run training-risk audit and then cross-checked against current repo evidence that many advanced heads are present but still weakly fed, default-off, or semantically incomplete. | Pass 1: this row survives because it captures a real training-risk doctrine that is broader than any single target-family row and more concrete than a vague “too many heads” complaint. | Pass 2 + 3: keep the wording explicit that the real issue is density/interference gating, not raw parameter count; the row should read as “archive-proposed activation discipline responding to visible sparse-head risk,” not as a claim that those gate formulas are already implemented. | medium | later | medium | medium | yes | fallback if sparse search heads keep starving | This row is about activation discipline, not just model size. The visible reason for the caution is that the repo already contains many advanced surfaces, but several remain sparse, semantically weak, or underfed enough that turning them on without density/transfer gates would risk negative transfer. |

## Appendix — archive file -> outcome

### Skipped files

- `reference_prompt_example_001_narrow_focused.md` -> skipped from extraction corpus
- `reference_prompt_example_002_broad_novel_fuse_loop.md` -> skipped from extraction corpus
- `reference_prompt_example_003_balanced_narrow_not_overconstrained.md` -> skipped from extraction corpus

### Processed files

- `answer_1_combined.md` -> merged away into another canonical row; broad docs-first handoff, no surviving canonical row after later stricter files
- `answer_1-1_combined.md` -> contributed surviving claim(s); early world-aware Hand-EV / unified-belief precursor used as predecessor support
- `answer_2_combined.md` -> contributed surviving claim(s); early public-teacher / privileged-vs-public target taxonomy and safety residual precursor support
- `answer_2-1_combined.md` -> contributed surviving claim(s); early narrow advanced-target-closure tranche and `exit_target`/`delta_q`/`safety_residual` boundary support
- `answer_3_combined.md` -> contributed surviving claim(s); compute-budget and program-coherence precursor support
- `answer_3-1_combined.md` -> contributed surviving claim(s); projected-belief object, reserve-shelf sequencing, and exactification staging precursor support
- `answer_5-1_combined.md` -> contributed surviving claim(s); budget-router/queue-scorer predecessor support for the tiny ponder-scorer lane
- `answer_6-1_combined.md` -> contributed surviving claim(s); runtime-first compression seam predecessor support
- `answer_7_combined.md` -> contributed surviving claim(s); safe search-distillation / `delta_q` target-closure predecessor support
- `answer_7-1_combined.md` -> contributed surviving claim(s); conservative ExIt / lower-confidence search-teacher predecessor support
- `answer_7-2_combined.md` -> merged away into another canonical row; lower-confidence teacher-distillation material folded into narrower ExIt/teacher rows rather than kept standalone
- `answer_8_combined.md` -> no surviving canonical claim; prompt-format / output-style analysis, not Hydra technical canon
- `answer_8-1_combined.md` -> contributed reject/block claim(s); precursor for the later-rejected posterior-consensus ExIt family
- `answer_8-2_combined.md` -> contributed surviving claim(s); action-sufficient CT-SMC world-compression predecessor support
- `answer_9_combined.md` -> contributed surviving claim(s); earlier broad target-activation draft kept mainly as contradiction evidence against later narrower target activation
- `answer_10_combined.md` -> contributed surviving claim(s); tiny ponder-scorer survivor and broad-router block
- `answer_11_combined.md` -> contributed surviving claim(s); narrow runtime-only compression survivor and benchmark-first framing
- `answer_12_combined.md` -> contributed reject/block claim(s); canonical reject source for posterior-consensus ExIt and regime-coupled opponent filtering
- `answer_13_combined.md` -> contributed surviving claim(s); long-run training-risk, density-gate, and conservative-budget claims added in this pass
- `answer_14_combined.md` -> contributed surviving claim(s); Hand-EV semantic repair, world-aware Hand-EV staging, and specialist endgame exactification
- `answer_15_combined.md` -> contributed surviving claim(s); target-provenance taxonomy, keep-off calls, and oracle interpretation
- `answer_16_combined.md` -> merged away into another canonical row; earlier rollout-threshold draft retained only as superseded contradiction evidence behind stricter `answer_16-1`
- `answer_16-1_combined.md` -> contributed surviving claim(s); stricter rollout trust boundary, retained `top2_policy_gap < 0.10`, and rejected-threshold cleanup
- `answer_17_combined.md` -> contributed surviving claim(s); Hand-EV naming/semantic mismatch and H1a repair detail
- `answer_18_combined.md` -> contributed surviving claim(s); strongest target-semantic audit and later narrowing of target activation claims
- `answer_19_combined.md` -> contributed surviving claim(s); exact narrow seam for the tiny learned scorer for the next ponder quantum
- `answer_20_combined.md` -> contributed surviving claim(s); cache/provenance authority critique and learner-only safe-policy recommendation
