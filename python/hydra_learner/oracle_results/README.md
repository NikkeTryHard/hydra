# Oracle results

Preserved JSON results from `/home/cachybtw/dev/hydra-pytorch-oracle` probe and `/home/cachybtw/tmp` artifacts before deleting temporary worktree.

Historical oracle probes kept for provenance only. Current production Python BC path uses Pixi `py-train` with PyTorch `2.11.0+cu128` / CUDA 12.8; do not use these old CUDA 13/nightly numbers for normal workflow or throughput decisions.
